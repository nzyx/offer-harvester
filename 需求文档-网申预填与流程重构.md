# 面邀收割机 · 需求确认文档 v1.1

> **主题**：网申预填 + 流程重构 + API 稳定性
> **状态**：需求已确认（定稿）｜模块 C 已完成｜**任务 1 结构化简历抽取已完成**｜**任务 2 向导式三步 UI 已完成**｜**任务 3 页面抓取 JD 已完成**
> **日期**：2026-09-14
> **基线版本**：v1.0.6 → v1.1.0（进行中）

---

## 一、已确认的决策

| # | 决策点 | 结论 | 直接影响 |
|---|--------|------|----------|
| 1 | 网申预填技术路线 | **通用语义引擎** | 不逐站维护；覆盖任意网申页；准确率中等，需用户核对 |
| 2 | 提交行为边界 | **只填不提交** | 代码层不触碰 submit；填充后高亮标记待核对字段 |
| 3 | 流程形态 | **向导 + 快捷入口** | 主线「简历 → JD → 产出」，保留「只想生成打招呼语」快捷路径 |
| 4 | API 稳定性范围 | **本次仅错误诊断细化** | 超时 / 重试 / 流式 / 断点续跑暂不纳入本次 |

---

## 二、模块 A · 网申预填

### A1 用户故事

> 我在某公司网申页填了十几分钟，还要再填一遍实习经历——点侧边栏的「填充当前页面」，字段自动填好，我核对一下自己点提交。

### A2 技术链路（四步）

```
① 结构化简历（离线，一次性）
   上传简历 → 提取纯文本 → AI 抽取为结构化字段 → 存 chrome.storage.local

② 字段识别（页面侧，content script）
   扫描表单控件 → 采集语义线索（label 文本 / placeholder / aria-label / name / id / 上下文文字）

③ 语义映射（本地规则优先）
   线索 → 标准字段（同义词规则表 + 编辑器类型推断）→ 置信度分级

④ 写入与标记
   仅写值 + 派发原生事件 + 高亮已填字段 + 生成「未识别字段」报告
```

### A3 必须解决的技术坑（重点）

| 坑 | 说明 | 对策 |
|---|---|---|
| **React/Vue 受控组件不认直接赋值** | 直接改 `input.value` 后，框架的虚拟 DOM 状态没变，提交时值会丢或校验报错 | 写入后必须派发 `input` + `change` 事件（必要时用原生 setter 绕过 React 的 value 拦截） |
| **字段语义靠 name/id 匹配准确率极低** | 各站命名风格完全不同（`userName` / `applicant_name` / `field_12`） | 以 **可见 label 文本** 为主线索，placeholder 次之，name/id 仅作辅助 |
| **自定义下拉框（非原生 select）** | 大量网申站用 div 模拟下拉（如「学历」） | 识别为自定义控件时默认跳过并提示用户手填，不强行模拟点击 |
| **附件上传无法程序化写入** | 浏览器安全限制，无法给 `<input type=file>` 赋值 | 明确提示「简历附件请手动上传」，不做无效尝试 |
| **iframe 内表单** | 部分平台表单在 iframe 中 | content script 已配 `all_frames: true`，可覆盖；需处理跨 frame 消息回传 |

### A4 结构化字段清单（草案 · 待确认）

**第一组 基础信息**：姓名 / 性别 / 出生日期 / 手机 / 邮箱 / 现居城市 / 籍贯 / 政治面貌
**第二组 求职意向**：期望岗位 / 期望城市 / 期望薪资 / 到岗时间 / 求职类型（实习、校招、社招）
**第三组 教育经历**：学校 / 学历 / 专业 / 学院 / 起止时间 / GPA 或排名 / 培养方式
**第四组 工作与实习**：公司 / 职位 / 起止时间 / 工作内容 / 业绩成果（可多条）
**第五组 项目经历**：项目名 / 担任角色 / 起止时间 / 项目描述 / 技术栈 / 成果（可多条）
**第六组 技能与补充**：语言等级 / 技能标签 / 证书 / 自我评价 / 作品链接

> **敏感字段**（身份证号、银行卡、紧急联系人、家庭住址）**默认不填**，需用户在设置里显式开启才纳入填充范围。

### A5 隐私边界（重要）

- 结构化简历全部存本地，**不上传任何服务器**（延续现有承诺）
- 字段映射**第一期采用纯本地规则表**，完全不外发页面内容
- 若后续启用「AI 兜底映射」（把未识别字段名发给 AI 判断），会在设置页**单独开关 + 明确告知**：页面字段名会发往**你自己配置的** AI 接口
- 填充行为不写日志到任何远端；仅在侧边栏内展示「本次填了哪些字段」

---

## 三、模块 B · 流程重构

### B1 结构对比

| | 现状 v1.0.6 | 目标 v1.1 |
|---|---|---|
| 顶层结构 | 双 tab 平级（岗位分析 / 简历工具） | **向导三步 + 快捷入口** |
| 简历 | 可选 | **主线必填**，但快捷入口可绕过 |
| JD | 必填（≥20 字） | 必填，新增「从当前页面抓取 JD」 |
| 产出 | 面试准备 / 打招呼语 / 简历优化 | 上述三项 **+ 网申预填** |

### B2 目标流程

```
① 上传简历
   → 解析纯文本（复用现有 PDF/Word/TXT 解析）
   → AI 抽取结构化字段
   → 展示「结构化预览」，用户可逐项修正 ← 这一步是网申预填的数据源

② 岗位 JD
   → 手动粘贴，或「从当前页面抓取」（自动提取网页 JD 正文）

③ 选择产出
   → 面试准备 / 打招呼语 / 简历优化 / 网申预填
```

**快捷入口**：首页常驻「跳过简历，直接生成打招呼语」，不强制走完向导。

### B3 数据模型变更

| 存储键 | 现状 | 目标 |
|---|---|---|
| `savedResumeText` | 纯文本 | 保留，并新增 `savedResumeProfile`（结构化 JSON） |
| `savedJob` | JD 文本 | 保留 |
| 新增 `profileVersion` | — | 结构化字段的 schema 版本，便于后续迁移 |
| 新增 `resumeLibrary` | — | 多份简历（第二期，见待确认项 3） |

> 迁移原则：老用户首次打开时，若检测到 `savedResumeText` 而无 `savedResumeProfile`，提示「一键重建结构化信息」，不静默丢弃。

---

## 四、模块 C · API 稳定性（本次范围：错误诊断细化）— 已完成实施

### C1 现状问题

当前 `callApi()` 只有一刀切错误提示：`生成失败，请检查设置后重试`。用户无法判断到底是 Key 错、额度用尽、模型名拼错，还是本地推理服务没启动。

### C2 错误分类映射表（要实现的核心）

| 触发条件 | 人话提示 | 可操作建议 |
|---|---|---|
| HTTP 401 / 403 | API Key 无效或没有该模型的权限 | 重新复制 Key；确认账号已开通该模型 |
| HTTP 404 | 接口地址或模型名不存在 | 核对地址结尾是否为 `/v1`；核对模型名与文档完全一致 |
| HTTP 429 | 请求太频繁或额度已用尽 | 等 1 分钟再试；到服务商后台查余额与限流 |
| HTTP 400 且响应含 model | 模型名称不被该服务识别 | 直接给出「请核对模型名」并附当前填入的名字 |
| HTTP 5xx | AI 服务端故障 | 非你配置问题，稍后重试；可换服务商 |
| `TypeError: Failed to fetch` | 网络不通 / 地址写错 / 本地服务未启动 | 检查网络；本地模型请确认 `127.0.0.1:端口` 已启动 |
| 响应 200 但内容为空且含 `reasoning_content` | 模型开了思考模式，配额被思考吃光 | 关闭思考模式或换非思考模型（已有逻辑，保留） |
| `finish_reason === 'length'` | 输出被 max_tokens 截断 | 提示调大上限或精简输入 |
| **请求长期无响应** | ⚠️ **当前无超时兜底，页面会一直转圈** | **建议补 90s 超时**（见待确认项 4） |

### C3 配套改动

- 设置页「测试连接」升级为**「连接诊断」**：一次点击输出上面分类结论 + 实际返回片段，不再只说「失败」
- 生成失败时，错误卡片直接给出「下一步该做什么」，而不是笼统重试

### C4 实施记录（2026-09-14 已完成）

| 文件 | 改动 |
|---|---|
| `common.js` | 新增 `API_TIMEOUT_MS = 90000`、`resolveCompletionUrl()`、`describeTimeoutError()`、`describeNetworkError()`；重写 `describeHttpError()`（补 5xx / 408 / 504 / 400 模型名分支，并附服务端返回片段） |
| `popup.js` | `callApi()` 加 90 秒超时兜底（严格区分「用户取消」与「超时」两种情况）；网络层与响应体异常翻译成人话；`finish_reason === 'length'` 给出截断提示；地址补全委托给 `common.js`（消除与设置页的重复实现）；顶层错误卡片直接呈现已翻译消息，不再叠加笼统建议 |
| `options.js` | 「测试连接」→「连接诊断」，复用 `resolveCompletionUrl`，加同款超时与分类输出，按钮状态用 `finally` 保证恢复 |
| `options.html` | 按钮文案与引导说明同步更新 |

**验证方式**：`node --check` 全量语法校验通过；用 `new Function` 加载 `common.js` 后逐条调用诊断函数，覆盖 401 / 429（额度 vs 限流）/ 400 模型名 / 404 / 500 / 网络层不可达 / 响应非 JSON / 请求超时 共 8 类场景，输出均符合预期（含 3 种接口地址写法的补全结果）。

---

## 五、分期建议

### 第一期（MVP · 跑通闭环）

1. 向导式三步 UI 重构 + 快捷入口
2. 结构化简历抽取 + 可编辑预览
3. 通用语义引擎预填（本地规则表）+ 只填不提交 + 高亮标记 + 未识别字段报告
4. 错误诊断细化 + 连接诊断

### 第二期（增强）

5. 「从当前页面抓取 JD」
6. 多份简历库（按岗位切换）
7. 投递记录 / 投递看板
8. 高频站点适配层（提升准确率）
9. 流式输出、超时重试、断点续跑

---

## 六、风险登记

| 风险 | 等级 | 说明 | 缓解 |
|---|---|---|---|
| 预填字段填错位置 | **高** | 语义引擎按置信度填，可能张冠李戴 | 只填高置信度字段；中低置信度仅高亮提示不写入；强制用户核对 |
| 站点反自动化检测 | 中 | 部分平台检测脚本注入行为 | 纯前端写入，不模拟点击提交；不隐藏来源；低频操作 |
| 敏感信息误填 | **高** | 身份证等被填到公开页面 | 敏感字段默认关闭，需显式开启 |
| 流程变重导致流失 | 中 | 只想快速生成面试题的体验变重 | 快捷入口常驻；简历非强制 |
| 无超时导致卡死 | **高** | 模型不响应时界面永久转圈 | 建议本次顺手补上（见待确认项 4） |
| 结构化抽取失真 | 中 | AI 抽取的字段与真实简历不符 | 预览可编辑；以用户确认为准 |

---

## 七、已确认的细节（定稿）

| # | 确认项 | 结论 |
|---|---|---|
| 1 | 预填字段范围 | **六组字段全要**；敏感字段（身份证、银行卡、家庭住址等）保留为「默认关闭的可选项」 |
| 2 | 从当前页面抓取 JD | **纳入第一期**，复用现有注入通道，省掉手动复制 |
| 3 | 多份简历库 | 第二期（第一期先单份跑通） |
| 4 | 请求超时兜底 | **纳入本次**（90 秒硬超时），已随模块 C 实施完成 |
| 5 | 投递记录 | 第二期（网申功能的自然延伸） |

---

## 八、下一步施工顺序（第一期）

| 顺序 | 任务 | 依赖 | 可独立验证 |
|---|---|---|---|
| 0 | 模块 C 错误诊断 + 超时兜底 | — | ✅ 已完成 |
| 1 | 结构化简历抽取（AI 抽取六组字段 + 预览可编辑） | 无 | ✅ 已完成（字段抽取是否正确） |
| 2 | 向导式三步 UI（简历 → JD → 产出）+ 快捷入口 | 任务 1 | ✅ 已完成（交互是否顺畅） |
| 3 | 「从当前页面抓取 JD」 | 复用现有注入通道 | ✅ 已完成（打分权重有回归保护） |
| 4 | 通用语义引擎预填（字段识别 → 语义映射 → 写入 → 高亮标记） | 任务 1、2 | 是（在真实网申页实测） |
| 5 | 端到端联调 + 老数据迁移（缺结构化信息时提示重建） | 全部 | 是 |

---

## 九、任务 1 实施记录（2026-09-14 已完成）

### 9.1 交付内容

| 文件 | 类型 | 改动 |
|---|---|---|
| `profile.js` | 新增 | 结构化简历数据层。六组字段 schema、AI 返回值归并、文本解析、本地规则兜底、预填清单、路径读写、存储读写。**无 DOM 依赖**，可在 node 下直接测试 |
| `prompts/resume-extract.md` | 新增 | 结构化抽取提示词 |
| `prompts.js` | 修改 | 加载新提示词，导出 `RESUME_EXTRACT_PROMPT` |
| `popup.js` | 修改 | 抽出统一请求层 `requestCompletion()`；新增结构化面板交互（解析 / 预览 / 编辑 / 增删条目 / 敏感字段开关 / 保存 / 清空）；老用户迁移提示 |
| `popup.html` | 修改 | 简历区新增结构化面板；引入 `profile.js` |
| `styles.css` | 修改 | 结构化面板样式，沿用黑白 + 红设计系统与衬线标题 |
| `tests/profile.test.js`、`tests/render.test.js` | 新增 | 无依赖验证脚本 |
| `manifest.json` | 修改 | 版本 v1.0.6 → v1.1.0 |

### 9.2 关键决策

**① 抽取结果用「`## 分组` + `字段名：值`」文本格式，不用 JSON**

初版提示词要求 AI 输出严格 JSON，与项目既定原则「禁止要求 AI 输出 JSON」（JSON 一丁点逗号引号错误就全炸）冲突，且对「六组嵌套字段」这种结构，一次语法错误会导致整个功能失效。改为：

```
## 教育经历
学校：广东海洋大学
学历：本科
专业：新闻学

## 教育经历          ← 同组标题重复出现即视为下一条
学校：某某中学
学历：高中
```

解析规则（`parseProfileText`）：
- 按 Markdown 标题切块，块内按 `字段名：值` 取键值，字段名用中文标签 + 别名表匹配
- 同组标题重复出现、或块内同键重复出现，都识别为「下一条经历」
- 无法识别的行并入上一字段（AI 常把长内容换行）；无法识别的分组直接丢弃
- **宁缺勿错**：认不出就留空，绝不猜
- 解析失败不报错给用户，而是降级到本地规则解析并如实说明原因
- 容错：个别模型坚持输出 JSON，也能被解析（作为最后的兜底分支）

**② 统一请求层**：`callApi()` 原本嵌在 `generate()` 内部，结构化抽取无法复用。抽出为模块级 `requestCompletion()`，超时 / 取消 / 错误翻译全项目只有一处实现，避免两套逻辑走偏。

**③ 未配置 AI 时用本地规则兜底**：正则抽取手机号、邮箱、姓名、学校、学历、专业、GPA、起止时间、公司、技能标签、语言、证书、自我评价、链接。只填空缺、不做语义猜测；解析失败不写入无效文本。

**④ 敏感字段（身份证 / 银行卡 / 住址 / 紧急联系人）**：可以抽取并存在本机，但默认在预览里隐藏、且默认不进 `flattenProfile()` 的预填清单，需用户显式打开开关。

### 9.3 验证结果

无依赖脚本，共 146 条断言全部通过：

| 脚本 | 断言数 | 覆盖 |
|---|---|---|
| `tests/profile.test.js` | 109 | 空结构、脏数据归并、顶层平铺兜底、路径读写、敏感字段过滤、预填清单、JSON 提取、本地规则解析、边界、文本格式解析（多条经历两种写法、标题变体、列表符号容错、注入安全） |
| `tests/render.test.js` | 37 | DOM 桩里真实加载 4 个脚本零错误、面板初始态、渲染 6 个分组、XSS 转义、敏感字段开关、编辑落盘、本地兜底、预填接口、老用户迁移提示 |

**开发中被测试抓出的真实缺陷（已修）**：

1. `createEmptyProfile()` 对对象型分组返回空 `{}`，而 `normalizeProfile()` 走的是补全全部键的 `normalizeItem()`——同一份数据两种形状，会让 UI 与预填踩空
2. 手机号正则不认 `138-0000-0000` 这种带分隔符写法
3. 表格化简历「学校 专业 学历 时间」同一行时抽不到专业
4. 为姓名排除章节标题加的词表含「语言」，把合法专业「汉语言文学」一起误杀（改用「以标题词开头」判定）
5. `loadMemory()` 的存储回调引用了稍后才声明的 `profile`，存在时序脆弱点（浏览器里因回调异步未暴露）

### 9.4 尚未覆盖 / 下个任务接手点

- 结构化简历数据的**消费方**（网申预填）在任务 4；消费接口 `flattenProfile(profile, { includeSensitive })` 已就绪并测通
- 结构化面板已随任务 2 并入向导第 1 步；顶层 tab 结构已移除
- 多份简历库为第二期，本次单份
- 抽取时简历原文按 12000 字符截断，超长简历需在后续提示或扩窗口

---

## 十、任务 2 实施记录（2026-09-14 已完成）

### 10.1 交付内容

| 文件 | 类型 | 改动 |
|---|---|---|
| `popup.html` | 重构 | 顶部双 tab（岗位分析 / 简历工具）→ **步骤条 + 三步面板**；简历区与 JD 区合入同一条主线；三个产出按钮升级为卡片；新增第 4 张「网申预填」卡（`hidden`，任务 4 启用） |
| `styles.css` | 改 | 替换 `.tabs` / `.tab` / `.tab-panel` 为 `.steps` / `.step-chip` / `.wizard-step` / `.step-collapsed` / `.output-card` 等；窄屏适配步骤条与产出卡 |
| `popup.js` | 改 | 新增向导模块：`WIZARD_ORDER` / `wizard` / `OUTPUT_CARDS` / `hasResume` / `jobReady` / `stepDone` / `stepSummary` / `updateOutputCards` / `renderWizard` / `setStep` / `gotoMissingStep` / `scrollToResults`；校验改为跳步；`loadMemory` 恢复上次步骤 |
| `tests/render.test.js` | 改 | 新增第 10 节「向导流程」34 项断言；DOM 桩补 `removeAttribute` / `scrollIntoView` |
| `tests/make-preview.js` | 新增 | 生成 `向导预览.html` 静态快照，用于无扩展环境时检查排版 |
| `CLAUDE.md` | 改 | 新增「向导结构约定」一节，含 TDZ 时序陷阱说明 |

### 10.2 形态与交互

| 行为 | 实现 |
|---|---|
| 三步推进 | 每步有「下一步」；步骤条可点击直接跳任意步（不锁步） |
| 快捷入口 | 第 1 步「跳过，先填岗位」；简历**不是必填**，只有打招呼语与简历优化需要 |
| 走过即折叠 | 离开的步骤收成一行摘要（文件名 / 字数 / JD 首行），带「修改」按钮展开 |
| 可用性前置 | 产出卡右侧直接写明「需先补岗位描述」/「需先补简历」/「可以生成」，不必点错才知道 |
| 校验跳转 | 点产出时若缺内容，自动切到缺的那一步并在该步内提示；用户一动手提示即消失 |
| 进度不丢 | 当前步骤写入 `chrome.storage.session.wizardStep`，关侧边栏重开回到原位 |
| 结果不藏 | 生成结果放在向导之外（向导下方），不随步骤折叠被隐藏；生成后自动滚入视野 |

### 10.3 验证结果

```
node tests/profile.test.js   109 通过 / 0 失败
node tests/render.test.js     71 通过 / 0 失败   ← 含新增向导 34 项
```

向导断言覆盖：三态互斥（展开 / 折叠 / 隐藏）、四种前置条件提示文案、两种校验跳转路径、提示自动清除、步骤条 chip 状态、步骤持久化、生成后滚动与完成态。

**开发中被测试抓出的真实缺陷（已修）**：`OUTPUT_CARDS` 声明在第 700 行附近，而 `loadMemory()` 在第 222 行调用——存储回调里 `renderWizard → updateOutputCards` 直接命中 TDZ。与任务 1 的 `profile` 是同一类问题，已把顶层状态统一前移并写进 `CLAUDE.md`。

### 10.4 下个任务接手点

- 第 3 步已预留 `#autofill-button` 卡片与 `#state-autofill`，任务 4 只需启用并接入 `flattenProfile()`
- `OUTPUT_CARDS` 是产出卡前置条件的唯一来源，新增产出只需在此追加一项

---

## 十一、任务 3 实施记录（2026-09-14 已完成）

### 11.1 交付内容

在岗位页一键抓取 JD 正文并回填输入框，省掉手动复制粘贴。

| 文件 | 状态 | 内容 |
|---|---|---|
| `jd-extract.js` | 新增 | 提取算法层（纯函数）：候选打分 `scoreJdCandidate`、选优 `pickBestCandidate`、文本清洗 `cleanJobText`、候选收集 `collectJdCandidates`、端到端 `extractJobText` |
| `jd-grab.js` | 新增 | content script：只应答主 frame 的 `jd:grab` 消息 |
| `manifest.json` | 改 | 新增 content_scripts 项（`jd-extract.js` + `jd-grab.js`，`all_frames: false`）；**未新增任何 permission**；v1.1.0 → **v1.2.0** |
| `popup.js` | 改 | 抓取流程、可用性刷新、撤销状态机；`grabUndo` / `grabBusy` 按 TDZ 约定前移 |
| `popup.html` | 改 | `#fetch-jd` 去掉 `hidden` 并从导航行移到输入框下方；新增 `#grab-undo` |
| `styles.css` | 改 | `.grab-row` |
| `tests/jd-extract.test.js` | 新增 | 62 项：打分权重回归 + 清洗规则 + DOM 层端到端 |
| `tests/render.test.js` | 改 | 补 `chrome.tabs` 桩；新增抓取状态机 31 项 |

### 11.2 提取策略

**两级候选 + 打分选优**，不做逐站适配：

1. 语义容器（`[class*=job-detail]` / `[class*=require]` / `article` …）
2. 兜底候选：`scope` 自身、`scope` 的直接子块、`body` 与 `body` 的直接子块

第 2 级的作用是**保证选择器写漏也不会导致整页失败**——只要 JD 位于某个顶层块内就仍能抓到，代价只是准确率下降。

打分信号（权重从高到低）：关键词命中 → 链接密度惩罚 → 元素语义 class/id → 长度 → 段落结构 → 文本密度。

**实测分数分隔**（`tests/jd-extract.test.js` 的固定样本）：

| 候选 | 分数 | 判定 |
|---|---|---|
| JD 容器 | **73.8** | 胜出 |
| 职位列表 | -0.2 | 落选 |
| 页脚 | -42 | 落选 |
| 导航区 | -45.3 | 落选 |

阈值 `25`，位于「结构干净但无语义类名的正文块」与「列表页」之间的边界。

### 11.3 开发中被校验抓出的缺陷（已修）

1. **`body` 靠长度优势压过真正的 JD 容器**（手算校验发现）：整页文本天然最长，会拿满长度分，结果把导航、页脚、推荐位一起抓进来。加了结构惩罚（`body` -12 / `main` -3），并配套**豁免重试**——页面级容器因惩罚落选时，去掉惩罚再判一次，避免「整页就是一个 JD」的简单页面被误报抓不到。
2. **`ROOT` 判定阈值需要两侧回归保护**：只有「JD 必须胜出」的断言不够，同时写死「导航 / 列表 / 页脚必须落选」，否则调一个系数就会让任一侧悄悄失效。
3. **候选数量必须有上限**：`innerText` 会触发排版计算，对整页上千个元素逐个调用会明显卡顿，用 `JD_MAX_CANDIDATES` 限制。

### 11.4 不做的部分（明确边界）

- **不覆盖 iframe 内的 JD**：只注入主 frame，少数自建招聘站把 JD 放在 iframe 里会得到「没找到岗位描述」并引导手动粘贴，不做无效重试
- **不处理需要点击展开的 JD**：详情折叠时提示用户先点开「职位详情」再抓
- **不处理图片渲染的 JD**：OCR 不在本期范围，直接引导手动复制
- **不做模糊回退**：清洗后不足 60 字直接报「太短」，不尝试返回未清洗文本（宁可少清也不塞一堆噪音给用户）

### 11.5 验证结果

```
node tests/profile.test.js      109 通过 / 0 失败
node tests/render.test.js       102 通过 / 0 失败   ← 含新增抓取状态机 31 项
node tests/jd-extract.test.js    62 通过 / 0 失败   ← 新增
```

抓取断言覆盖：消息协议（类型 / 目标标签页 / `frameId: 0`）、成功回填与来源提示、覆盖时的撤销、撤销还原、手动改动后撤销自动失效、三类失败原因的差异化文案、非网页页面的禁用与不发送消息、切回网页后恢复、标签页切换监听。

### 11.6 下个任务接手点

- 页面注入通道已跑通，任务 4 的预填复用同一协议，新增能力沿用 `jd:` 前缀即可
- 抓取到的 `url` / `site` 已随响应返回，任务 4 的「投递记录」（第二期）可直接复用
- 若任务 4 需要覆盖 iframe，改 `manifest.json` 的 `all_frames` 并在 popup 侧去掉 `{ frameId: 0 }`

---

## 十二、任务 4 实施记录（2026-09-14 已完成）

### 12.1 交付内容

| 文件 | 职责 | 行数 |
|---|---|---|
| `form-fill.js` | 语义引擎（纯算法层，无 DOM，node 下可测） | 912 |
| `form-fill-page.js` | content script：采集特征 / 写入值 / 红框标记 | 494 |
| `prompts/form-map.md` | AI 表单语义映射提示词 | 99 |
| `prompts.js` | 加载第 6 个提示词 `FORM_MAP_PROMPT` | +2 |
| `popup.js` | 扫描 → 计划 → 渲染 → 确认 → 清除 五段状态机 | +430 |
| `popup.html` | 产出卡 04 + 预填面板（head / 两张清单 / 两开关 / 两按钮 / 免责说明） | +47 |
| `styles.css` | `.autofill-*` 与 `.af-item*` 全套样式 | +180 |
| `manifest.json` | 第 4 个 content_script（`form-fill-page.js`）+ 版本 1.3.0 | — |
| `tests/form-fill.test.js` | 153 项断言 | 新增 |
| `tests/render.test.js` | 第 12 节预填流程 72 项断言 | +200 |

**权限零新增**：`permissions` 仍是 `storage` / `sidePanel` / `tabs`，`host_permissions` 沿用既有 `http(s)://*/*`。

### 12.2 架构：为什么分两层

语义判断（纯函数）与 DOM 触达（不可测）严格分离：

```
form-fill-page.js                   popup.js                form-fill.js
  采集控件特征 ──── form:scan ────→ 本地打分 ────────────→ classifyFillField
                                     ↓ 认不出的字段
                                  AI 语义映射（可选）
                                     ↓
                                  创建填写计划 ──────────→ createFillPlan
                                     ↓ 用户确认
  写入值 + 红框  ←──── form:fill ─── { ref, value }[]
```

`ref`（`'ff' + 序号`）是两层之间的唯一句柄，页面侧维护注册表。元素在扫描后脱离文档时如实报 `stale`，不静默失败。

这样做的收益：153 项语义断言能跑在 node 里，用真实样本压测试；DOM 层只剩「取值 / 派发事件 / 加 class」这类无需测试的机械动作。

### 12.3 双引擎：本地规则为主，AI 为增强

**本地规则**（`FILL_RULES`，41 条）覆盖六组字段。每条规则有：

- `hi` 高置信关键词 / `lo` 低置信关键词（需章节或控件类型佐证）
- `types` 允许的控件类型
- `not` 排除词——**只在 `attr` / `label` 层生效**，用于挡住「公司规模」「学校性质」这类被泛词误命中的字段

打分是三层 haystack 加权（`attr` 1 / `label` 0.85 / `near` 0.45）+ 锚点/精确/章节/控件类型加减分，最后过**阈值 + 分差双门槛**：

- 低于 `7.5` → 认不出（`unknown`）
- 与次优解分差小于 `0.9` → 含义不明确（`ambiguous`）

两者都交给用户手填并说明原因。

**AI 映射**（可选）只处理本地认不出的字段，且角色被刻意限制为「从固定语义枚举里选一个」，输出 `序号<TAB>语义` 或 `?`。**AI 只决定「这个字段是什么意思」，填什么值完全由本地取值层决定**——语义判错只是少填一项，值取错是事实性错误。AI 的判断以 `hint` 形式参与打分，不覆盖本地结果。

AI 未配置 / 超时 / 返回不合法 → 退回纯本地，并在状态行如实告知「有 N 个字段本地认不出，连接 AI 后可提高识别率」。

### 12.4 提交边界（硬性）

**只填不提交**不是约定而是代码事实：不存在任何点击提交按钮、触发 `form.submit()` 或按回车提交的路径。`form:fill` 的消息体只有 `{ ref, value }`，`tests/render.test.js` 对此有断言：

```js
check('填写消息只带 ref 与 value', fillMsg.message.items.every(i => Object.keys(i).sort().join(',') === 'ref,value'));
check('填写消息不含提交类字段', !/submit|click|form\.submit/i.test(JSON.stringify(fillMsg.message)));
```

### 12.5 开发中被校验抓出的缺陷（已修）

1. **`search` 阻断词误杀 `researchExperience`**：拉丁词必须按词边界匹配，改成正则 `(^|[^a-zA-Z])(...)([^a-zA-Z]|$)`，中文另用子串表。
2. **`公司规模` / `学校性质` / `公司行业` 被泛词误命中**：这些字段含「公司」「学校」但填的不是公司名/学校名。引入 `not` 排除词机制 + `ruleDisqualified()`，且**只看 `attr` / `label` 层不看 `near`**——同区块其它字段的文字会造成误伤。
3. **`projects.description` 的 `lo` 是空数组**：只有两字「描述」的字段认不出。补 `lo: ['描述', '简介']`，同时把 `experience.description` 的 `lo` 从 `['职责','工作']` 扩为 `['职责','工作','描述','简介']`（靠章节归组消歧）。
4. **`<label>性别 <select><option>男</option></select></label>` 的标签解析**：`textContent` 会把 option 全文当标签。改用 `ownTextOutsideControl()`，排除 `INPUT/TEXTAREA/SELECT/BUTTON/SCRIPT/STYLE` 后只取文本节点。
5. **下拉框清单显示 `option value` 而非选项文字**：`<option value="1">男</option>` 写入 `"1"`，清单也显示 `"1"`，用户无法核对。新增 `fillDisplayValue()`，select 回传 `optionText`。这条是渲染测试抓出来的。
6. **扫描进度文案会冲掉结果摘要**：进度定时器已 `clearInterval`，但已排队的 tick 仍会改写 `#autofill-summary`。加 `autofillProgressGen` 计数，停止后的 tick 一律丢弃。
7. **`tests/render.test.js` 的脚本加载列表漏了 `form-fill.js`**：`createFillPlan is not defined` 只在运行时暴露。已在 `CLAUDE.md` 记下「加载列表必须与 `popup.html` 的 `<script>` 顺序一致」。

### 12.6 明确不做的部分

- **不做逐站适配器**：招聘网站改版频繁，一家一个适配器维护不过来。走通用语义引擎，任何标准表单元素都能用
- **不写自定义下拉框**（`div` 模拟的 select）：写入 `input.value` 不会进入组件状态，跳过并提示手填（原因码 `custom-select`）
- **不写文件上传**（`input[type=file]`）：浏览器安全模型禁止脚本赋值，跳过并提示手填
- **不猜**：选项匹配找不到就返回 `null`（`no-option`）；单字值不参与包含匹配；`strict` 字段超长直接拒绝（`too-long`）而不是截断；日期只精确到年月而控件要完整日期时明确告知，不默认补 `01` 后假装成功
- **不覆盖已有内容**：默认不覆盖，`overwrite` 开关打开才写
- **不写敏感字段**：默认关闭，`FILL_DEFAULT_SENSITIVE` 兜底
- **不覆盖 iframe 内的表单**：只注入主 frame，提示用户展开后重新扫描

### 12.7 验证结果

```
node tests/form-fill.test.js    153 通过 / 0 失败   ← 新增：11 个段落
node tests/profile.test.js      109 通过 / 0 失败
node tests/jd-extract.test.js    62 通过 / 0 失败
node tests/render.test.js       174 通过 / 0 失败   ← 含新增预填流程 72 项
```

预填断言覆盖：正向 35 项必认出、反向 8 项必认不出、阻断规则与词边界、下拉选项匹配（精确/别名/近似/不匹配）、日期与值适配、经历条数推进（两种真实表单写法）、端到端 26 字段填写计划、每个原因码都有文案、规则 id 无重复、加载零错误、面板渲染与转义、开关联动（敏感 / 覆盖）、只填不提交、三类写入失败（`stale` / 写失效 / 无通道）、清除标记、非网页页面、切换标签页清空计划。

### 12.8 下个任务接手点

- 本地实测一轮真实网申站（MokaHR / 北森 / 智联 / 猎聘的表单），记录识别率与失败模式
- 若某类字段普遍认不出，优先扩 `FILL_RULES` 的 `lo` 词表，而不是上 AI
- 若某站标签结构特殊（如纯 `div` 布局无 `label`），考虑扩 `form-fill-page.js` 的标签解析优先级，而不是加站适配器
- 任务 5：端到端联调 + 老数据迁移

---

## 十三、简历提取方案重做（2026-09-14 已完成）

用户反馈：「提取方法有点问题，像项目经历与实习经历都有缺失」，并附上真实简历。
拿真实简历跑了一遍，问题比反馈的更严重。

### 13.1 三个根因（用真实简历定位，非推测）

| # | 根因 | 实测症状 |
|---|---|---|
| 1 | **`extractPdfText` 用 `items.map(i => i.str).join(' ')`**，完全忽略 y 坐标 | **整页压成一行**，行结构彻底丢失。`专业` 被填成 `手机号码`；教育经历只剩学校 |
| 2 | **实习只抽公司名**（代码注释写着「内容留给用户补充」） | 实习的**职位、起止时间、工作内容全丢**，只剩公司名 |
| 3 | **`profile.projects` 从未被赋值** | **项目经历 100% 缺失** |

根因 1 是源头：本地规则与 AI 提示词都按行处理，行结构一丢，两边同时失效。

### 13.2 修复

**新增 `resume-text.js`（文本还原层，纯函数可在 node 下测）**

- `rebuildPdfLines(items)`：按 y 坐标聚类成行 → 行内按 x 排序 → 按 x 间距补空格。
  空格是字段间的断词依据，少了它「姓名：刘子涵居住地：深圳」会被读成一个名字。
- **续行合并**：PDF 硬换行会把一句话切断（`…文档制` + `作，累计输出…`），不合并则工作内容是断的、主修课程被截断。合并需同时满足 7 个条件：
  行距 ≈ 正常行距（取众数）／**上一行「排满」**（右边界 ≥ 内容宽度 0.9）／起始 x 相同／上一行不以句末标点结尾／下一行不是「字段名：」／上一行不是条目头／下一行不含日期范围。
  其中「排满」是关键——它是区分「被换行切断的句子」与「上下相邻的独立条目」的信号，独立条目写不满一行。
- `normalizeResumeText`：TXT/MD/DOCX 来源统一换行符、去行尾空白、折叠空行。

**重写本地规则的经历提取**（`profile.js`）

- 支持**两种日期排版**：行首 `2026.06-2026.8 某公司 产品实习生`、行尾 `某公司 产品实习生 2025.07-2025.10`。日期位置决定头部文字取哪一段。
- **月份放宽到 3 位**：真实简历里 `2025.012-2026.03` 这种笔误很常见，按意图纠正为 `2025.12`。
- **「小标题 + 要点」整段聚合成 `工作内容`**，小标题本身保留，用 `；` 连接；边界靠「下一个含日期的行」或「下一个章节标题」。
- **项目经历新增提取**：项目名 + 角色（含「学员-TOP20」连后缀、角色单独占一行的情况）。
- **无章节标题时兜底**：整篇扫但只认含公司后缀的条目，避免把学校当公司。

### 13.3 补的网申字段（先调研再定，不是拍脑袋）

调研了校招网申（含央国企 / 银行系统）的必填项，补 6 个当前缺失的：

| 字段 | 为什么必须有 |
|---|---|
| `basic.nativePlace` 生源地 | 央国企/银行必填，且常用于属地岗位匹配。**与籍贯是两个字段**（生源地=高考户籍地） |
| `education.rank` 专业排名 | 与 GPA 在网申里是两个独立输入框；「3/120」比「前 5%」更有说服力 |
| `education.courses` 主修课程 | 中石油等系统在「其他描述」栏明确要求填核心课程 |
| `experience.department` 部门 | 大公司网申实习栏必填 |
| `experience.industry` 所属行业 | 央国企网申必填 |
| `skills.honors` 荣誉奖项 | 独立于证书（网申是两个栏目），且要按含金量分级填 |

**拆分旧字段时同步清理了冲突**——这批字段原本混在别的字段里，不清理会让值填错框：

- `basic.hometown` 的 `hi` 里含 `生源地` → 移除（否则生源地被当成籍贯）
- `education.gpa` 的 `hi` 里含 `专业排名`/`成绩排名` → 移除
- `skills.certificates` 的 `hi` 里含 `荣誉奖项`/`获奖情况` → 移除
- `PROFILE_KEY_ALIASES.college` 的 `department` 别名 → 移除（它是实习的「部门」，不是校内的「学院」）
- `form-fill.js` 里上述关键词同步清理

还顺带修了一个反向缺陷：`resume-extract.md` 原先要求「空值也要保留字段行」，六组约 45 行里大半是空的，纯属白烧输出额度。改为**只输出有值的字段**后，输出 token 少了约一半。

### 13.4 开发中被真实数据与测试抓出的缺陷（已修，供避免复发）

1. **`2025.012-2026.03 某机器学习项目` 被当成「项目经历」章节标题**（测试抓出）：含日期范围的行必须先排除在章节标题之外，否则**整条经历被消耗掉、什么都不剩**。
2. **`GPA：3.7（专业前20%）` 把下一行的 `主修课程：…` 吞掉**：上一行以右括号结尾，而右括号不属于句末标点。加「下一行是字段名则不合并」。
3. **条目头吞掉描述首句**：合并会让项目名把「AI Agent 介入下品牌触达路径变化研究」这类描述首句也吃进去。加「上一行是条目头则不合并」。
4. **`产品负责人` 被拆成「产品」+「负责人」**：「产品」留在项目名里。改成整体匹配。
5. **连续几行同缩进的短条目被合并成一整行**（测试抓出的规则缺陷）：续行还有个必要条件是「上一行排满」。
6. **`专业` 抽不到**：`lines.find(l => l.includes(school))` 只取第一行含学校名的行，而「出生年月：2005年6月 毕业院校：广东海洋大学」排在教育行之前、学校名又落在行尾，把真正含专业的那行挤掉了。改为遍历所有候选行。
7. 项目角色「独立作者」单独占一行时漏抽。

### 13.5 验证结果

```
node tests/common.test.js        47 通过 / 0 失败
node tests/resume-text.test.js   40 通过 / 0 失败   ← 新增
node tests/profile.test.js      140 通过 / 0 失败   ← 新增 31 项（经历提取）
node tests/form-fill.test.js    187 通过 / 0 失败   ← 新增 34 项（新字段互斥）
node tests/jd-extract.test.js    62 通过 / 0 失败
node tests/render.test.js       249 通过 / 0 失败
────────────────────────────────────────────────
合计                            725 通过 / 0 失败
```

**用户真实简历（产品-广东海洋大学-刘子涵.pdf）实测**：

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 已填项数 | 13 | **35** |
| 教育经历 | 1 段（专业错抓成「手机号码」） | 1 段（含 GPA 3.7、专业前20%、9 门主修课程） |
| 实习经历 | 2 段（**只有公司名**） | 2 段（公司 + 职位 + 起止 + 完整工作内容） |
| 项目经历 | **0 个** | **2 个**（名称 + 角色 + 起止 + 描述） |
| 荣誉奖项 | 混在证书里 | 独立字段，6 项 |

### 13.6 新增诊断工具

```bash
node tests/check-resume.js 简历.pdf          # 端到端体检，报出提取率与各组字段
node tests/check-resume.js 简历.pdf --text   # 额外打印重建后的文本
```

用途：单元测试能锁住规则本身，锁不住「这份简历抽出了多少」。本次改造中「项目经历整段丢失」「专业被填成手机号码」都是靠真实简历才暴露的。**改了 `resume-text.js` 或提取逻辑后，除了跑单元测试，建议拿几份真实简历跑一遍这个工具。**

### 13.7 下个任务接手点

- 建议再找 2–3 份不同模板的简历（表格型、左右分栏型、英文简历）跑 `check-resume.js`，记录失败模式
- **左右分栏型简历目前不支持**：`groupPdfRows` 只按 y 聚类，同 y 的两栏内容会被并到一行。若实测遇到，需按 x 先分栏再分别重建
- 若某类字段普遍抽不到，优先扩 `LOCAL_*` 词表，而不是上 AI

---

## 十四、字段排查能力与静默跳过修复（2026-09-14 已完成）

用户报障：「我的学校为什么抓不到呢」，附北森（`hollyland.zhiye.com`）网申页链接。

### 14.1 排查过程与结论

**先试静态抓取**：外壳 HTML 只有 46KB 配置壳，`学校`/`school`/`input`/`select` **出现 0 次** ——
表单由北森的 `ux-recruitment-portal-2022-form-pc` 组件库在客户端渲染，静态拿不到结构。

**再试真实浏览器**：`agent-browser` 在本环境无法启动 Chromium（连 `example.com` 也是 SIGTERM、无任何输出），走不通。

**改为让扩展自己说话**：这个方向其实更好 —— 用户的实际环境（登录态、已填了一半的表单）本来就无法在我这边复现，让扩展把「扫描到的全部控件 + 识别结果 + 跳过原因」导出来，信息比我在浏览器里看更准确。

### 14.2 顺带修掉一个真实缺陷：字段无声消失

`FILL_SILENT_REASONS = ['option-not-match']`，而 `option-not-match` **没有对应的文案**
（`FILL_REASONS` 里只有 `no-option`）。后果：单选/复选类字段一旦选项文字与简历内容对不上，
就**被静默跳过** —— 字段在「需你手动处理」清单里凭空消失，用户完全不知道发生了什么。

修复：

- 补 `option-not-match` 文案（「选项对不上」）
- `FILL_SILENT_REASONS` 清空为 `[]`，所有原因码一律显示
- 加两条断言兜住这类**静默失效**：
  - 扫源码查 `FILL_REASONS` 有没有**重复 key**（JS 静默取最后一个，不报错）
  - 用真实计划驱动一遍，要求每条手动项都有 `title` + `detail` + 已定义的原因码

（本次改动中我自己就踩了一次重复 key，被断言挡住了。）

### 14.3 新增：诊断详情导出

预填面板底部新增「有字段没填上？导出诊断详情」，一键把报告复制到剪贴板：

```
【网申预填诊断报告】
页面：https://hollyland.zhiye.com/form?...
站点：hollyland.zhiye.com
时间：2026-09-14 15:30:00
扫描到 42 个可编辑控件｜将填写 18｜需手动 12

── 控件清单（按页面顺序，含未识别与已跳过的）──
序号 | 类型 | 标签 / name | 识别结果 | 处置
  1 | text           | 姓名 [name=realName]          | basic.name(15.2)  | 将填写
  2 | custom-select  | 学校 [name=school]            | education.school(14.1) | 跳过：自定义下拉框
  3 | text           | 兴趣爱好                       | —                 | 未识别
...

── 需你手动处理的原因 ──
- [custom-select] 学校：自定义下拉框 — 这类下拉框由页面脚本模拟，写入不会生效，请手动选择。

── 提示 ──
· 有 3 个自定义下拉框：这类控件由页面脚本模拟，扩展无法可靠写入（写了不进组件状态），只能手动选择。
```

**为什么必须列出「扫描到的全部控件」**：控件可能压根没被扫描到（在 iframe 里、被 `isVisible`
过滤、`SCAN_SELECTOR` 没命中），那种情况**两张清单里都不会出现**，只看清单永远分不清
「没扫到」与「识别失败」。这是这个功能的全部价值所在。

剪贴板被拒时把报告打到控制台兜底，不让用户白点一次。

### 14.4 「学校抓不到」的可能原因（供用户对照诊断报告判断）

| 情况 | 报告里的表现 |
|---|---|
| 学校是**自定义下拉框**（中大型招聘系统最常见的做法） | 出现在控件清单，处置列显示「跳过：自定义下拉框」 |
| 控件在**iframe / 弹窗**里，扫描时还没渲染 | **压根不在控件清单里** |
| 控件是纯 `div`、没有 `role=combobox` 等 ARIA 属性 | 不在清单里（`SCAN_SELECTOR` 命中不了） |
| 扫到了但标签结构特殊，语义识别不出 | 在清单里，识别结果列为「—」，处置列「未识别」 |
| 识别出了但被 `has-value` / 敏感开关挡下 | 在清单里，处置列显示对应原因 |

### 14.5 验证结果

```
node tests/common.test.js        47 通过 / 0 失败
node tests/resume-text.test.js   40 通过 / 0 失败
node tests/profile.test.js      140 通过 / 0 失败
node tests/form-fill.test.js    194 通过 / 0 失败   ← 新增 7 项（静默失效防线）
node tests/jd-extract.test.js    62 通过 / 0 失败
node tests/render.test.js       265 通过 / 0 失败   ← 新增 16 项（诊断导出）
────────────────────────────────────────────────
合计                            748 通过 / 0 失败
```

### 14.6 下个任务接手点

- 拿到用户的诊断报告后，按 14.4 的表定位并决定改法：若是「没扫到」要改 `SCAN_SELECTOR` 或放开 iframe 注入；若是「自定义下拉框」则评估是否值得做模拟点击选择（成本较高、跨站脆弱）
- `agent-browser` 在本环境无法启动 Chromium，后续若需要看真实页面结构，需先解决环境问题或改用其他方式

### 14.7 追补：区分「可搜索」与「只读」的自定义下拉框（同日晚些）

继续逆向北森的表单组件库（`pcform-*.chunk.min.js` 的 webpack 分片映射表）后拿到关键信息：

```
87: beisen-phoenix-part2     99: standard-lookup-advanced   421: standard-department-selector
637: standard-lookup         628: standard-file-uploader      974: standard-user-selector
```

`standard-lookup` 是北森的**查找型字段**组件（用于选择学校、部门这类实体）——
这基本确认了院校字段不是原生 `<select>`。

**但随之暴露一个原来被一刀切掉的问题**：同为「自定义下拉框」，处理方式本应不同：

| 类型 | 特征 | 正确处理 |
|---|---|---|
| **可输入** | Element UI / antd 的搜索型 Select、lookup 的搜索框 | **填入关键词**触发组件自身筛选，用户点一下即可 |
| **只读** | 点击才弹窗、输入框不接受键入 | 真的写不进去，跳过 |

原来的实现把两者都跳过，等于放弃了前者的巨大可用性提升（用户要在几百所学校里翻找）。

**改动**：

- `form-fill-page.js`：新增 `isSearchableCustomSelect(el, type)` → `input` + 非 `readOnly` + 非 `disabled`；字段特征新增 `searchable`
- `form-fill.js`：`blockFillField` 只跳过 `!searchable` 的 `custom-select`；类型加分把 `custom-select` 视同 `combobox`（同一个 ARIA 角色的两种实现，否则会被按「类型不符」无谓扣分）
- `createFillPlan`：可搜索的标记 `assisted: true`，note 说明「请从下拉候选里点选才算选中」
- `popup.js`：结果提示里单列「其中 N 项是可搜索的下拉框，已填入关键词，请从下拉候选里点选确认」；诊断报告处置列显示「填入关键词(待点选)」；提示段新增一条

**为什么必须标出 `assisted`**：我们只是"帮填了关键词"，用户没点选就不算填好。
不标出来，用户会以为填好了直接提交，那个值会丢 —— 这比不填更糟。

顺带把只读那类的文案说清原因：「这类下拉框由页面脚本接管、输入框不接受键入，无法自动填写」。

**验证**：`form-fill.test.js` 194→**204**（新增第 13 节 10 项：只读跳过 / 可搜索不跳过 / 缺 `searchable` 时向后兼容 / 类型加分等效 / assisted 标记与提示文案）。
另用 `new Function` 抽出 `isSearchableCustomSelect` 单独验了 5 种输入组合（可输入 / 只读 / 禁用 / div 模拟 / 普通输入框），结果全部符合预期。

全量：47 + 40 + 140 + 204 + 62 + 265 = **758 通过 / 0 失败**。

---

## 十五、诊断报告驱动的第二轮修复（2026-09-14 已完成）

用户在真实网申页（北森 `hollyland.zhiye.com`）点了「导出诊断详情」并回传报告：
**扫描到 60 个可编辑控件｜将填写 12｜需手动 48**。报告直接推翻了我上一轮的假设 ——

### 15.1 上一轮的假设是错的

上一轮我判断「学校」属于**没被扫到**（在 iframe 里 / 选择器没命中），于是加了 `standard-lookup`
识别与「可搜索 / 只读」分流。但报告显示：

- 60 个控件里有 **40 个 type=text、标签显示「请选择」/「请输入」**，学校几乎必然在其中之一
- **学校不是没扫到，是扫到了但标签没读出来** —— 标签位被占位符「请选择」占住了

我拿报告里的分数逐个反推，把真实特征钉死了：

| 报告行 | 报告分数 | 本地复现的特征组合 | 结论 |
|---|---|---|---|
| 姓名 | 9.6 | `label=姓名` + `placeholder=请选择` | 真实特征就是「标签 + 无语义占位符」 |
| 专业名称 | 11.6 | `label=专业名称` + `placeholder=请选择` | 同上 |
| 邮箱 | 认不出 | `label=邮箱` + `placeholder=请选择` → **认不出** | 与报告完全一致 |
| 中国大陆 | basic.phone 9.1 | 邻近的国家选择框文字被当成了标签 | 真实误填 |

**这一步是整轮的转折点。** 报告只写了「未能识别」，是分数反推给出了真实特征 —— 也正因如此，
本轮把「未填写控件的原始特征」加进了报告，下次不必再反推。

### 15.2 四个根因

| # | 根因 | 证据 |
|---|---|---|
| 1 | **标签解析在组件库排版下全线落空** | 北森/antd/Element UI 把标签与控件放在同一个 form-item 容器里，标签既不是控件的兄弟节点、也不是祖先 `<label>`，现有 5 种策略全部拿不到，只剩占位符 |
| 2 | **无语义占位符占据了标签位** | 标签位解成「请选择」后，既不是有效标签、又挡掉了后面可能管用的反推策略 |
| 3 | **弱词被占位符稀释后永远够不到阈值** | 「邮箱」在 `lo`（弱基础分 2.5），`squash('邮箱请选择')` 拿不到「整段匹配」奖励，只有 5.6 分 < 7.5。中文里最标准的邮箱写法反而认不出 |
| 4 | **国家/地区选择框被误判成手机号** | 它是 `div role=combobox`，不含原生控件，躲过了所有类型判断；它的显示文字「中国大陆」还被当成了手机号的标签 |

### 15.3 修复

**标签解析（`form-fill-page.js`，本轮的主要工作量）**

- 新增 `labelFromBlock()` + `textBeforeControl()`：**取所在容器里「排在控件之前」的文字**。
  标签几乎总排在控件前面，而控件自己的显示值（下拉框里的「请选择」、已选中的「本科」）排在后面 ——
  按文档顺序一刀切，比事后减噪声可靠。由内向外试 4 层，取第一个「短（≤24 字）且非占位符」的结果。
- 新增 `isGenericText()`：判定「请选择 / 请输入 / 请填写 / 选填 / --」这类**无信息量文字**。
  判住之后视为「没找到标签」继续往下试。**关键分辨**：「请输入姓名」含字段名，必须保留。
- `textFromPrecedingSibling()` 加 `CONTROL_ISH_SELECTOR` 判断，且**连兄弟自身一起判**
  （`sib.matches(...)`）—— 北森的国家选择框是 `<div role="combobox">`，里面没有原生控件，
  原来的 `querySelector` 只看后代就会漏掉它的显示值。
- `labelFromBlock` 的候选还要按空格切词、剔除占位符词（「学校 请选择」→「学校」）。
- 新增 `nearbyTextFromBlock()`：标签确实反推不出来时，把区块文字交给 `form-fill.js` 的
  「近邻」弱信号层 —— 那一层此前 `nearbyText` **从未被赋值，一直是空转**。

**打分（`form-fill.js`）**

- 新增 `FILL_WHOLE_LABEL_BONUS`：「**标签层的整段文字恰好就是这个关键词**」单独给奖励。
  表单给字段起名就是这么短。弱词只能靠它单独成立。**只认整段，不认包含**（有断言保护）。
- `buildFillHaystacks()` 用 `isGenericFillText()` 过滤无语义占位符，不再让「请选择」稀释标签层。

**安全（`form-fill.js` / `form-fill-page.js`）**

- 新增 `region` 阻断：标签只由地区名构成（中国大陆 / 内地 / 香港 / 澳门 / 台湾 / 境内 / 境外 / 海外）
  或就是国家类词（国家 / 国籍 / 地区 / 区号 / 国家和地区）→ 判为地区选择框跳过。
- `fieldTypeOf()` 把 `aria-haspopup` / `aria-expanded` 也作为「脚本接管控件」的判据
  （北森 `standard-lookup`、antd Select 都带）。这类 input 当普通文本框写进去会
  「看起来填上了、其实没选中」，提交时值丢掉。原生 `date` / `month` 优先于这两个属性。

**字段与章节缺口（报告里 9 个真实标签认不出）**

实践名称 → `projects.name`；实践描述 / 职务描述 → `projects.description`；
职务 → `projects.role`（在项目区块）/ `experience.title`（在实习区块），章节未知时判「含义不明确」；
获奖项 / 获奖描述 → `skills.honors`；技能名称 → `skills.skillTags`；证书描述 → `skills.certificates`。
章节关键词补 `实践经历 / 社会实践 / 校园实践 / 实践项目`（北森的「实践经历」独立于「项目经历」）。

### 15.4 诊断报告升级

- **主表的标签列不再拿 `placeholder` 兜底**：改为 `label || '(标签为空)'` 并单独显示 `[ph=...]`。
  两者混在一起会掩盖「标签根本没读出来」这个关键事实 —— 本轮的 40 行「请选择」就是这么被误读的。
- 新增「**未填写控件的原始特征**」段：逐个列出 `类型 | 只读 | role | label | ph | aria | name | nearby | 章节`，
  只列未填写的（要排查的正是它们）。空值用 `∅` 显示，不留白。
- **重复的手动原因合并计数**（`×N`）：不合并的话 40 行一模一样的「未能识别」会把要看的那几行淹掉。
- 新增两条提示：地区选择框的数量、以及**「有 N 个控件没能读到字段名 —— 不是你的简历缺内容」**。
  后者很重要：用户看到「未能识别」会以为是自己简历缺东西，其实是扩展读不到字段名。

### 15.5 验证

```
node tests/common.test.js         47 通过 / 0 失败
node tests/resume-text.test.js    40 通过 / 0 失败
node tests/profile.test.js       140 通过 / 0 失败
node tests/form-fill.test.js     252 通过 / 0 失败   ← 新增第 14 节 48 项（真机报告驱动）
node tests/jd-extract.test.js     62 通过 / 0 失败
node tests/render.test.js        273 通过 / 0 失败   ← 新增 12.14b 9 项（诊断报告）
node tests/form-fill-page.test.js 51 通过 / 0 失败   ← 新增（内容脚本首次有测试）
─────────────────────────────────────────────────
合计                             865 通过 / 0 失败
```

`tests/form-fill-page.test.js` 是**内容脚本的第一个测试**。此前这一层一行测试都没有，
而「学校抓不到」恰好出在这里。它用最小 DOM 桩（不引入任何依赖）在 node 下跑标签解析，
覆盖：北森式 form-item 反推、内层优先（外层容器含多个字段时不粘连）、右置标签、
「自身是 combobox 的兄弟不当标签」、纯占位符容器必须返回空、9 种常见排版的回归。

`manifest.json` 版本 → **1.5.0**，权限仍零新增。

### 15.6 下个任务接手点

- **请用户在真实页面重跑一次诊断报告**：这次报告会写明学校字段的 `label` 到底读成了什么、
  输入框是否只读、`role` 是什么。本轮的所有修复都是基于分数反推 + 代码推断，
  只有新报告能确认「学校」是否已经落进「将填写」清单。
- 若学校已进「将填写」但标注「待点选」→ 属于可搜索的查找型控件，去页面点一下候选即可。
- 若仍显示「标签为空」→ 说明该控件的标签结构比 form-item 更特殊，需要看新报告里的
  `label / ph / aria / name / nearby` 四要素才能决定下一步（可能是标签用了图片、或纯 CSS 伪元素）。
- 60 个控件里仍有约 20 个是「重复空行」（同一区块的多条备用经历），目前会逐条进手动清单。
  后续可考虑按「同章节同语义的重复空块」折叠，减少清单噪音。
