# 面邀收割机 · 需求确认文档 v1.2

> **主题**：字段表重构为网申表单镜像 + 重新解析智能合并 + 补充引导
> **状态**：**已实现并全绿**（1196 条断言通过，0 失败）
> **日期**：2026-09-16
> **基线版本**：manifest v1.5.3 → v1.6.0
> **前置文档**：`需求文档-网申预填与流程重构.md`（v1.1，已完成）

---

## 一、问题定义

用户反馈：「AI 预填会因为简历解析不全导致填不全」。

排查结论：**问题不在预填算法，在字段表本身**。三个根因：

| # | 根因 | 证据 |
|---|------|------|
| 1 | 字段表只有 6 组，参考网申表单有 12 栏 | 民族、在校职务、在校实践、附加信息这四类**表里没有位置**，AI 抽到也无处可放；技能/获奖/语言/证书只是「一坨文字」，而表单要的是多条结构化字段 |
| 2 | 空白字段不可见 | `prompts/resume-extract.md` 要求「只输出有值的字段，空字段整行省略」，用户看不出漏了什么，不知道该补哪些 |
| 3 | 重新解析覆盖手工补充 | `popup.js: parseProfile()` 里是 `profile = extracted` 直接替换，手工补的几十项会被一次解析全部冲掉，所以「存本地、换简历只改改」这个用法跑不通 |

---

## 二、已确认的决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 字段表结构 | **完整重构为 12 组镜像**，与网申表单逐栏对齐 |
| 2 | 重新解析处理 | **智能合并，手工改动优先**：AI 新值只更新「非手工」字段，用户改过的字段保留 |
| 3 | 补充引导 | **顶部「还缺 N 项」+ 分组角标 + 必填标记** |

---

## 三、目标字段表（12 组）

分组顺序即参考表单顺序。`*` 为网申必填项（`PROFILE_REQUIRED_KEYS`）。
分组 id 沿用既有英文命名，**只改 label 不改 id**，把改动面压到最小。

### 1. `basic` 个人信息（对象型）

| key | 字段名 | 必填 | 说明 |
|---|---|---|---|
| name | 姓名 | * | |
| phone | 手机号码 | * | |
| email | 邮箱 | * | |
| gender | 性别 | * | 男 / 女 / 保密 |
| birthDate | 出生日期 | * | |
| idCard | 证件号码（身份证） | | 敏感，默认关闭 |
| photo | 证件照 | | **文件类：无法程序化上传**，纳入字段表仅作提醒 |
| degree | 最高学历 | | 可从教育经历带出（见 §5.4 派生回退） |
| degreeLevel | 最高学位 | | 新增。博士 / 硕士 / 学士 / 无 |
| major | 专业名称 | | 可从教育经历带出 |
| ethnicity | 民族 | | **新增** |
| school | 毕业学校 | | 可从教育经历带出 |
| nativePlace | 生源地 | | 高考户籍地，≠ 籍贯 |
| politicalStatus | 政治面貌 | | 扩展字段（参考表单无，央国企常用） |
| hometown | 籍贯 | | 扩展字段 |
| currentCity | 现居城市 | | 扩展字段 |
| bankAccount | 银行卡号 | | 敏感 |
| homeAddress | 家庭住址 | | 敏感 |
| emergencyContact | 紧急联系人 | | 敏感 |

### 2. `intent` 求职意向（对象型）

| key | 字段名 | 必填 |
|---|---|---|
| targetSalary | 期望月薪（税前） | |
| targetCity | 期望工作城市 | * |
| availableDate | 到岗时间 | |
| targetPosition | 期望岗位 | |
| jobType | 求职类型 | |

> 后两项为扩展字段：参考表单没有，但打招呼语与简历优化都要用，保留。

### 3. `education` 教育经历（列表）

`school` 学校名称\* · `college` 学院名称\* · `major` 专业名称\* · `degree` 学历\* · `startDate` 开始时间\* · `endDate` 结束时间\* · `gpa` 成绩(GPA) · `rank` 专业排名 · `courses` 主修课程 · `studyMode` 培养方式

### 4. `experience` 实习经历（列表）

`company` 单位名称 · `title` 职位名称 · `startDate` 开始时间 · `endDate` 结束时间 · `description` 实习内容 · `department` 部门 · `industry` 所属行业 · `achievement` 业绩成果

### 5. `projects` 项目经历（列表）

`name` 项目名称 · `role` 职务 · `startDate` 开始时间 · `endDate` 结束时间 · `description` 项目描述 · `techStack` 技术栈 · `achievement` 成果

### 6. `campusRole` 在校职务（列表）**新增**

`title` 职务名称 · `startDate` 开始时间 · `endDate` 结束时间 · `description` 职务描述

### 7. `campusPractice` 在校实践（列表）**新增**

`name` 实践名称 · `startDate` 开始时间 · `endDate` 结束时间 · `description` 实践描述

> 现在这类内容被硬塞进 `projects`（`FILL_SECTION_KEYWORDS.projects` 里写着「实践经历 / 社会实践 / 校园实践」），语义不通，还会与真正的项目经历抢填同一个表单栏。

### 8. `extra` 附加信息（对象型）**新增**

| key | 字段名 | 说明 |
|---|---|---|
| hobby | 兴趣爱好 | |
| strength | 特长 | |
| selfEvaluation | 自我评价 | 扩展字段（原 `skills.selfEvaluation`） |
| portfolio | 作品 / 主页链接 | 扩展字段（原 `skills.portfolio`） |

> **与参考表单的偏差（有意为之）**：参考表单的「附加信息」带「添加附加信息」按钮，是可重复块组件。这里按**对象型**处理，因为它本质是"一人一份"，做成列表会让用户每填一次都得先点「+ 添加一条」。

### 9. `skills` 技能（列表）**由文本串改结构化**

`name` 技能名称 · `level` 掌握程度 · `years` 使用时间总计 · `description` 技能描述

- `level` 候选：精通 / 熟练 / 掌握 / 了解 / 入门

### 10. `honors` 获奖情况（列表）**由文本串改结构化**

`name` 获奖项 · `date` 获奖时间 · `level` 获奖级别 · `description` 获奖描述

- `level` 候选：国家级 / 省部级 / 市级 / 校级 / 院级
- 排序沿用：国家级 > 省部级 > 校级，同级别按时间倒序

### 11. `languages` 语言能力（列表）**由文本串改结构化**

`type` 语言类型 · `level` 掌握程度

- `type` 候选：英语 / 日语 / 韩语 / 法语 / 德语 / 粤语
- `level` 自由文本（可写「CET-6 / 读写流利」）

### 12. `certificates` 证书（列表）**由文本串改结构化**

`name` 证书名称 · `date` 获得时间 · `description` 证书描述

---

## 四、数据迁移（v1 → v2）

`PROFILE_SCHEMA_VERSION` 提到 `2`，新增 `migrateProfile(raw)`，在 `normalizeProfile()` 入口按 `version` 判断后调用。**旧数据一项都不能丢。**

| 旧位置 | 新位置 | 规则 |
|---|---|---|
| `basic.*` | `basic.*` | 字段名不变，新增 key 补空 |
| `intent.*` `education.*` `experience.*` `projects.*` | 同名 | 新增 key 补空 |
| `skills.skillTags` | `skills[]` | 按「、」拆成多条，只填 `name` |
| `skills.certificates` | `certificates[]` | 按「、」拆成多条，只填 `name` |
| `skills.languages` | `languages[]` | 按「、」拆成多条，只填 `type` |
| `skills.honors` | `honors[]` | 先按「、」再按「；」拆，只填 `name` |
| `skills.selfEvaluation` | `extra.selfEvaluation` | 直接搬 |
| `skills.portfolio` | `extra.portfolio` | 直接搬 |

拆分后只剩空白项则丢弃。必须单测覆盖「v1 完整数据 → v2 一个字段都不丢」。

---

## 五、智能合并（决策 2）

### 5.1 记录来源：脏标记

自动保存只说明"存没存"，不说明"是谁填的"。要支持合并，必须先知道来源。

- **对象型分组**：`item._dirty = ['name', 'email']` —— 该组里被用户手工改过的 key
- **列表型分组**：脏标记挂在条目上，且条目需要**稳定 id**（`item._id`）。不能按数组下标记脏 —— 用户删掉中间一条，后面所有下标都会错位，脏标记会串到别的经历上

### 5.2 条目对齐

重新解析后，AI 的结果是"一堆新条目"，要跟已有条目对上才能合并。对齐键按分组指定（`PROFILE_MATCH_KEYS`）：

| 分组 | 对齐键 | 说明 |
|---|---|---|
| `education` | `school` | 同一学校只留一段 |
| `experience` | `company` | |
| `projects` | `name` | |
| `campusRole` | `title` | |
| `campusPractice` | `name` | |
| `skills` | `name` | |
| `honors` | `name` | |
| `languages` | `type` | |
| `certificates` | `name` | |

对齐值做归一化（去空格、小写）比较，避免「某某有限公司」与「某某有限公司 」被当成两条。

### 5.3 合并规则

```
合并(新解析 parsed, 已有 existing):
  对象型分组:
    对每个字段 key:
      existing._dirty 含 key  → 保留 existing 的值（手工优先）
      否则 parsed 有值        → 用 parsed 的值
      否则                    → 保留 existing 的值（AI 这次没抽到，不要清空）
  列表型分组:
    对 parsed 的每条新条目:
      能按对齐键匹配到 existing 某条 → 逐字段合并（同上规则），保留该条的 _id 与 _dirty
      匹配不到                       → 追加为新条目（来源标记 ai）
    对 existing 里未被匹配到的条目 → 原样保留（不清空、不删除）
    _removed 名单里的条目不再追加（见下）
```

### 5.4 派生回退（解决"个人信息与教育经历重复填报"）

参考表单里「最高学历 / 专业名称 / 毕业学校」在个人信息栏问一遍，教育经历栏再问一遍。用户不该填两遍。

**取值层回退**：预填取 `basic.degree` / `basic.major` / `basic.school` / `basic.degreeLevel` 时，若为空则回退到 `education` 里**学历最高、时间最近**那一条的同名字段，并在填写清单里注明来源（「取自教育经历」）。

UI 上这几个输入框的 placeholder 写「留空则用教育经历里的」，用户补一处即可，两栏都能填上。

### 5.5 删除的条目不能"复活"

用户点了某条经历的「×」，重新解析时 AI 又把这条抽出来了 —— 如果不管，用户会以为删除没生效。

- 记 `profile._removed = [{ group, matchKey, value }]`，删除时写入
- 合并时，parsed 里命中 `_removed` 的条目直接丢弃，不追加
- 用户在字段表里手动加回同名条目时，从 `_removed` 里移除该记录

### 5.6 合并结果必须如实告知

沿用项目既有的「多分支文案先拼公共部分再各自补尾」约定，横幅要说清三件事：

- 更新了几项（AI 抽到新值）
- 保留了几项（你手工填过的）
- 新增了几段经历 / 有没有条目被你删过而没被拉回来

例：`解析完成 · 更新 12 项｜保留你手工填的 7 项｜新增 1 段实习`

---

## 六、补充引导（决策 3）

### 6.1 必填清单

`PROFILE_REQUIRED_KEYS` 以 `${groupId}.${key}` 形式定义，列表型分组对所有条目生效：

```
basic.name, basic.phone, basic.email, basic.gender, basic.birthDate,
intent.targetCity,
education.school, education.college, education.major, education.degree,
education.startDate, education.endDate
```

### 6.2 顶部统计

替换现有 `#profile-summary` 的文案：

`已填 42 项 · 还缺 18 项（其中必填 5 项）`

- 计数为 0 时不显示 `0`（沿用项目既有约定）——必填缺 0 就只说「必填已填齐」
- 旁边加一个「跳到下一处空缺」按钮，点一下滚到第一个空字段并聚焦

### 6.3 分组角标与必填标记

- 分组标题右侧角标沿用现有 `x/y` 形式（已有 `groupCountBadge`），`y` 改为「必填数 + 已填的非必填数」会让语义混乱，**保持"已填/总数"不变**，必填缺口单独用红点表示
- 必填字段标签上加红点（复用 `.is-sensitive` 的样式语言，新增 `.is-required`）

### 6.4 分组折叠（12 组必然很长）

- 分组标题可点击折叠 / 展开，状态存 `chrome.storage.session`
- 默认规则：**有内容的组展开，整组全空的组折叠**（用户一眼先看到已解析出来的东西，再决定补哪里）
- 「只看空缺」开关：勾上后隐藏所有已填字段，只留空框 —— 补字段时只看得见要补的，这是最实用的一项

---

## 七、AI 提示词变更（`prompts/resume-extract.md`）

| 变更 | 内容 |
|---|---|
| 字段清单扩写 | 从 6 组扩到 12 组全部字段名，新增民族 / 最高学位 / 在校职务 / 在校实践 / 附加信息，并把技能·获奖·语言·证书展开成四条结构化字段名 |
| 区分三组「做过什么」 | `在校职务`（学生会 / 班委 / 社团任职）、`在校实践`（志愿服务 / 社会实践 / 支教）、`项目经历`（课程项目 / 科研 / 个人项目）—— 明确给出判别口径，避免全部塞进项目经历 |
| 证件照 | 明确说明**不抽取**（简历里的照片是图片，抽成文字没有意义），该字段只由用户手动补 |
| 获奖级别 | 新增字段，按原文写；原简历没写级别就整行省略，不要替它断级别 |
| 技能掌握程度 | 原文没写就整行省略，不要按常识推断「熟练 / 精通」 |
| 「至今」 | `结束时间` 统一写 `至今`，预填侧会据此勾选表单的「至今」复选框 |
| `max_tokens` | 6000 → 8000（条目变多、四类改成多条结构，6000 有截断风险） |

保留不变：只抽不创作、空字段整行省略、`## 分组名` + `字段名：值` 纯文本格式、荣誉按含金量排序、籍贯≠生源地、GPA≠专业排名。

---

## 八、预填层变更（`form-fill.js` / `form-fill-page.js`）

### 8.1 新增字段规则

`FILL_RULES` 新增：`campusRole.title` / `campusRole.description` / `campusPractice.name` / `campusPractice.description` / `extra.hobby` / `extra.strength`，以及四类结构化字段（`skills.name` / `skills.level` / `skills.years` / `honors.name` / `honors.level` / `languages.type` / `certificates.name` 等）。

### 8.2 ⚠️ 章节关键词必须同步搬家（最容易漏的一步）

`FILL_SECTION_KEYWORDS` 现有归属里有**四处必须移走**，否则新老两组会互相抢字段：

| 关键词 | 现在在哪 | 应移到 |
|---|---|---|
| 实践经历 / 社会实践 / 校园实践 / 实践项目 / 实践信息 | `projects` | `campusPractice` |
| 其他信息 / 补充信息 | `skills` | `extra` |
| 技能证书 | `skills` | 拆开：「技能」→ `skills`，「证书」→ `certificates` |
| 获奖情况 / 荣誉奖项 / 语言能力 | `skills` | 分别 → `honors` / `languages` |

同时 `projects.name` 的 `hi` 里的「实践名称 / 实践项目 / 实践课题」要移给 `campusPractice.name`，`projects.description` 里的「实践描述 / 实践内容」要移给 `campusPractice.description`。

**改完必须跑 `tests/form-fill.test.js`** —— 反向用例（导航 / 页脚 / 同类泛词必须落选）是这次改动的安全网，比正向用例更重要。

### 8.3 新增「至今」复选框支持

表单里「结束时间」旁边的「至今」是个独立 checkbox。新增原因码 `ongoing`，规则：`endDate` 值为「至今」时勾选它，同时不往结束时间框里写「至今」这三个字。勾不上时进手动清单说明原因。

### 8.4 文件类字段的诚实处理

`basic.photo` 与任何文件上传控件，继续走既有 `file` 原因码，提示「浏览器不允许脚本写入文件，请手动上传」。纳入字段表的意义是**提醒用户别漏**，而不是假装能填。

---

## 九、测试与验证

| 测试 | 变更 |
|---|---|
| `tests/profile.test.js` | 大改：12 组结构、迁移（v1→v2 不丢字段）、脏标记、条目对齐、删除不复活、派生回退 |
| `tests/form-fill.test.js` | 新字段正向用例 + **章节关键词搬家后的反向用例**（旧词不得再命中新组） |
| `tests/render.test.js` | 新增 UI 断言：折叠默认态、只看空缺开关、必填红点、顶部缺失计数、合并结果横幅 |
| `tests/check-resume.js` | 拿 2-3 份真实简历跑，报出 12 组的提取率 |
| 真机验证 | 拿参考表单那套系统（北森）实际跑一次预填，比对 12 栏的填充率 |

**回归底线**：`CLAUDE.md` 记录的几个历史坑必须仍然成立 —— 分数阈值分隔（JD 73.8 / 导航 -45.3 / 列表 -0.2 / 页脚 -42）、`resolveLabel()` 的区块反推、区号框必须拦掉、字段绝不无声消失。

---

## 十、实施顺序

| # | 任务 | 交付物 |
|---|---|---|
| 1 | 数据层：schema v2 + 迁移 + 脏标记 + 合并 | `profile.js`（或拆 `profile-merge.js`）+ 单测 |
| 2 | UI：折叠 / 角标 / 必填红点 / 顶部缺失计数 / 只看空缺 | `popup.js` `popup.html` `styles.css` |
| 3 | 提示词：12 组清单重写 + 排版判别口径 | `prompts/resume-extract.md` |
| 4 | 预填：新语义规则 + 四类列表取值 + 至今勾选 + 派生回退 | `form-fill.js` `form-fill-page.js` |
| 5 | 真实简历体检 + 真机网申页验证 | 验证结论 |
| 6 | 文档同步 | `CLAUDE.md` 字段表结构 / 合并约定 / 必填清单 |

> 任务 1、3 可并行；任务 4 依赖 1（字段结构）与 3（AI 产出的键名）；任务 2 依赖 1。

---

## 十一、风险与对策

| 风险 | 对策 |
|---|---|
| 12 组让侧边栏太长，用户找不到重点 | 分组折叠 + 默认只展开有内容的组 + 「只看空缺」开关 |
| 章节关键词搬家改错，导致字段互相抢位 | 反向用例锁定；改动后拿真机页面复核预填清单 |
| 脏标记按数组下标记录，删条目后错位 | 条目加稳定 `_id`，脏标记挂条目上 |
| 合并把用户手工内容冲掉 | `_dirty` + 合并前不算覆盖；横幅如实报告"保留了几项" |
| 旧数据（v1）用户升级后字段表空白 | `migrateProfile()` + 「v1 完整数据 → v2 不丢字段」单测 |
| AI 对"在校职务 / 在校实践 / 项目经历"三者分不清 | 提示词给出明确判别口径 + 抽错也只是填错栏，用户可改 |

---

## 十二、实现结果

### 12.1 覆盖率实测（端到端）

拿参考表单的 **12 栏共 56 个控件**跑预填：

| 阶段 | 填充 | 未填原因 |
|---|---|---|
| 只跑 v1 旧简历（迁移后） | 29/56 | `no-value` × 25、`file` × 1（证件照）、`sensitive` × 1（身份证） |
| 把字段表里缺的补齐后 | **54/56** | 只剩 `file` × 1（证件照，浏览器不允许脚本写 file 控件）与 `no-value` × 1（该字段确实没填） |

12 个分组每一栏都能通。剩下的两项都不是缺陷：证件照只能手动上传，`到岗时间` 是测试数据里本来就没写。

### 12.2 实现与原方案的偏差

| 方案里的写法 | 实际实现 | 原因 |
|---|---|---|
| 未提及 | 新增 `PROFILE_GROUP_KEY_ALIASES`（分组内键名解析）与 `PROFILE_AMBIGUOUS_KEYS` | 「名称 / 描述 / 时间 / 掌握程度」在 12 组里大量同名，全局别名表会把实习的「描述」和项目的「描述」混成一个键 |
| 「基本信息 → 个人信息」 | 分组 id 仍是 `basic`，只改 label | 改 id 会牵动 `form-fill.js` 的规则表与章节词表，收益为零 |
| `certificates` 三条字段 | 实现另加了 `*_summary` 语义（`skills` / `honors` / `languages` / `certificates`） | 网申里这四栏常是「一个控件装下全部」，逐条语义填不进去 |
| 派生回退「在取值层回退」 | 由 `applyProfileDerived()` 产出派生副本，`structuredProfile()` 统一用它 | `form-fill.js` 不依赖 `profile.js` 是硬约束，派生逻辑必须留在数据层 |

### 12.3 上线前发现并修掉的三个缺陷

这三条都是**测试全绿但行为是错的**，靠端到端跑真实表单才暴露：

| # | 缺陷 | 后果 | 修法 |
|---|---|---|---|
| 1 | 派生值是死代码：`applyProfileDerived` 算出了 `basic.degree='硕士'`，但 `FILL_RULES` 里个人信息栏的学历/专业/学校全指向 `education.*` | 个人信息栏的「最高学历」填成 `education[0]` —— 本科在前、硕士在后的简历会被**填成本科**（事实性错误） | 新增「最高那一档」取值：`sectionGroup === 'basic'` 时按学历排名取条目，而不是取第 0 条 |
| 2 | 「最高学位」标签得分 7.1，卡在 7.5 阈值下面 → `unknown` | 用户在字段表里填了「学士」，网申的「最高学位」栏永远填不进去 | 新增独立语义 `basic.degree` / `basic.degreeLevel`，并给 `education.degree` 加 `not: ['最高']` |
| 3 | `prompts/form-map.md` 还停在旧的 24 个语义，而 `FILL_RULES` 已扩到 85 条；**没有任何测试盯着它** | AI 语义映射只能返回旧词，新分组的字段一律救不回来，**而且不报错** | 重写枚举段落，并加 `tests/form-fill.test.js` 第 20 节：语义不能漏、不能多、12 个分组不能缺 |

第 3 条是这次最值得记住的教训 —— **提示词与代码之间没有断言，就等于没有契约**。`resume-extract.md` 早就有「提示词与 schema 一致性」断言（`tests/profile.test.js`），`form-map.md` 却一直没有，于是它漂了两个版本没人发现。

### 12.4 顺带修掉的一处迁移质量问题

v1 的 `skills.languages` 常写成「CET-6、粤语」这种**只写等级、不写语言名**的样子。原迁移逻辑只认「英语 CET-6」这种语言名在前的写法，于是把 `CET-6` 直接当成「语言类型」搬了过去 —— 网申的语言类型下拉里是「英语 / 日语 / 粤语」，匹配不上就整栏填不进去（实测语言能力从 4/4 掉成 0/4）。

现在补了等级 → 语言名的反推表（CET / TEM / IELTS / TOEFL → 英语，JLPT / N1-N5 → 日语，等等，拉丁词走词边界避免误命中）。
