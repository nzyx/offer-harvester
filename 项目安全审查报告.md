# 面邀收割机 · 项目安全审查报告（v1.6.1）

> **审查对象**：Manifest V3 侧边栏扩展 v1.6.1（manifest.json version）。本报告覆盖 **v1.6.0 的字段表重构全链路**（R1–R3 的由来）与 **v1.6.1 的消息入口加固**（R4 的修复）
> **审查范围**：全部源码 + 提示词 + 数据流。本轮**新增覆盖** `profile.js` / `form-fill.js` / `form-fill-page.js` / `resume-text.js`，即「简历预填」全链路
> **审查日期**：2026-09-18
> **替代**：2026-08-26 版（v1.0.6）。旧版范围不含预填链路，且其「JD/简历/结果存 storage.session，不上传」的数据存储结论**已被 v1.6.0 推翻**
> **验证基线**：7 个测试套件 1208 项断言全绿（common 47 / resume-text 40 / profile 304 / form-fill 368 / form-fill-page 79 / render 308 / jd-extract 62）

## 结论

**代码本身没有可被远程利用的漏洞** —— 无 XSS、无注入、无未授权数据外传、无 RCE。

这个项目的风险性质不是「谁能偷走你的数据」，而是「**你把数据交给了谁**」。核心是三项需要知情的数据处理事实，其中两项是本轮重构新引入或范围扩大的：

| 性质 | 项目 | 说明 |
|---|---|---|
| **功能本质**（不是缺陷） | R1 预填写入目标页 DOM | 让浏览器把值填进表单，页面自然能读到 |
| **设计使然**（有规避手段） | R2 简历全文发给 AI 服务商 | 不配 API Key 即可完全规避 |
| **v1.6.0 新引入** | R3 字段表明文持久落盘 | 为实现「换简历只改差异」而必然产生 |

加固项中，**消息入口 sender 校验（R4）已于 v1.6.1 修复**并加了防回退断言。

---

## 一、为什么必须重审这一版

v1.5.3 → v1.6.0 的字段表重构，改变了两个安全前提：

1. **数据落盘面扩大**。旧版简历相关数据全部走 `chrome.storage.session`（关浏览器即清空）；v1.6.0 为实现「字段表保存本地、以后解析不同简历只改差异」，把 12 组字段表改为写 `chrome.storage.local`（**明文、持久**）。这是需求的必然结果，不是失误，但必须知情。
2. **审查范围必须先扩大**。旧报告的结论建立在「popup/options/background/copy-guard」之上，而预填链路的三个文件（`profile.js` 数据层、`form-fill.js` 语义层、`form-fill-page.js` 页面注入层）当时不在范围内。

另外，旧报告标记 P0 的两项（重定向偷 Key、Referrer 泄露招聘页 URL）**已于 v1.6.0 修复**，详见第五节。

---

## 二、数据流与信任边界

### 2.1 简历数据流

```
简历文件（PDF / DOCX / TXT）
  │  本地解析（pdf.js / mammoth / FileReader），不上传
  ▼
简历原文  ──── 存 storage.session（关浏览器即清空）
  │
  │  ★ 出站点 1：仅在配置了 AI 接口时发生
  ▼
AI 服务商  ←── 简历全文（截断 12000 字）在 popup.js:928 拼进 prompt
  │
  ▼
字段表 · 12 组  ──── 存 storage.local（明文持久落盘）
  │
  │  用户手工补充（手工改动优先，重新解析不覆盖）
  ▼
预填计划（本地匹配，零网络）
  │  只发匹配到的值 {ref, value}
  ▼
目标网申页 DOM  ←── ★ 出站点 2：写入 input.value，页面脚本可读
```

### 2.2 数据存储边界

| 数据 | 位置 | 生命周期 | 加密 | 代码位置 |
|---|---|---|---|---|
| 简历原文、JD、生成结果 | `storage.session` | 关浏览器即清空 | 无（内存） | popup.js:195 |
| **字段表（12 组）** | **`storage.local`** | **持久落盘** | **无（明文）** | profile.js:1945 |
| API Key | `storage.local` | 持久落盘 | 无（明文） | options.js:46 |

### 2.3 网络出口（全项目仅 2 处真网络请求）

| 位置 | 用途 | 外发内容 |
|---|---|---|
| popup.js:2771 | AI 调用 | 简历全文 / JD / 生成内容 + API Key |
| options.js:94 | 连接诊断 | 仅字符串 `'Hi'` + API Key |

补充说明：

- `prompts.js:14` 用 `chrome.runtime.getURL` 读取扩展内部文件，**不是网络请求**。
- **`form-fill.js` 与 `form-fill-page.js` 内没有任何 `fetch`** —— 预填这一环不上传任何内容。
- **AI 字段映射只发页面控件结构**：`buildFormMapPrompt`（popup.js:1429）外发的是控件的类型 / 标签 / placeholder / name / id / 选项文字，**不含任何用户数据**。附带效应是目标网申页的字段结构会到达 AI 服务商，需要知情。

---

## 三、风险总览

| # | 标签 | 问题 | 位置 | 严重度 | 建议 |
|---|---|---|---|---|---|
| R1 | `[数据外发]` | 预填把简历值写入目标页 DOM，页面脚本必然可读 | form-fill-page.js:625 | 中（功能本质） | 知情 + 界面提示，用前确认域名 |
| R2 | `[数据外发]` | 简历全文发给配置的 AI 服务商 | popup.js:928 等 4 处 | 中（设计使然） | 可用「不配 Key」完全规避 |
| R3 | `[本地落盘]` | 字段表明文持久落盘 | profile.js:1945 | 中 | 知情；定期清空字段表 |
| R4 | `[权限]` | 4 处消息入口不校验 sender | 见第五节 | 低（当前不可利用） | **✅ v1.6.1 已修** |
| R5 | `[权限]` | host_permissions 全站 + 允许 http | manifest.json | 中 | 看取舍（本地 LLM 依赖 http） |
| R6 | `[供应链]` | vendored pdf.js / mammoth 无版本声明 | lib/ | 低-中 | 升级并标注版本 |
| R7 | `[副作用]` | MAIN 世界覆盖 `Event.prototype.preventDefault` | copy-guard-main.js:23,31 | 中 | 看取舍（旧报告遗留） |
| R8 | `[本地落盘]` | API Key 明文落盘 | options.js:46 | 低（MV3 标准做法） | 知悉即可 |
| R9 | `[数据外发]` | 导出 HTML 含基于简历生成的内容 | popup.js:2685 | 低 | 知悉即可（需主动点击才产生） |

---

## 四、v1.6.1 已修：消息入口 sender 校验（R4）

### 问题

4 处 `chrome.runtime.onMessage` 监听**都没有校验消息来源**，其中 `form-fill-page.js` 的三个分支危害最大：

```js
if (type === 'form:scan')  { sendResponse(scanPage()); return; }   // 读走整页控件结构
if (type === 'form:fill')  { sendResponse(applyFill(message)); return; } // 触发填表
if (type === 'form:clear') { ... }
```

### 可利用性判断（重要，避免误报）

**当前不可利用**：网页脚本够不到 `chrome.runtime`；manifest 也没有声明 `externally_connectable`。Chrome 122+ 起跨扩展消息需接收方显式声明，因此其他扩展同样发不进来。

**但它是唯一一道防线**：一旦哪天为别的功能开放了外部消息通道，没有这道校验，任意网页即可触发填表（`form:fill`）、读走整页结构（`form:scan`）、读走页面正文（`jd:grab`）。

### 修复

4 处统一在函数体开头加校验，并注释说明理由：

```js
chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
  if (!sender || sender.id !== chrome.runtime.id) return;
  ...
```

| 文件 | 行号 | 拦截的消息 |
|---|---|---|
| form-fill-page.js | 781 | `form:scan` / `form:fill` / `form:clear` |
| jd-grab.js | 83 | `jd:grab` |
| copy-guard.js | 70 | `copyGuard:set` |
| background.js | 63 | `copyGuard:enable` / `disable` / `ready` |

### 防回退

这类 listener 只在真实扩展环境注册（node 下无 `chrome.runtime`），行为测不到，因此按项目既有做法加了**源码级断言**（`tests/form-fill-page.test.js` 第 7 节，12 条）：

- 每个文件都注册了消息监听
- 每个文件的消息入口都校验了 `sender.id`
- **校验必须排在第一个业务分支之前**（否则等于没防）

已做反转验证：临时移除校验后断言立即报 2 条失败，确认不是恒真断言。

---

## 五、逐项详情

### R1【中 · 功能本质】预填 = 把简历交给目标网站

`form-fill-page.js:625` 的 `setNativeValue` 通过 native setter 写 `el.value` 并派发 `input` / `change` 事件（这是为了让 React / Vue 等框架能感知到值变化）。**页面脚本必然能读到这些值** —— 这不是漏洞，是「填入」的定义。

真正的风险在于**在哪个网站填**：一个仿冒成北森 / Moka 样式的钓鱼网申页，只要骗用户点一次预填，姓名 / 手机 / 邮箱 / 教育经历 / 实习经历就全部交出。

缓解现状：`autofill-sensitive` 开关默认关闭，只挡住 4 个敏感字段（见第 6.1 节），其余字段照填。

**建议**：界面上把「点预填 = 向当前网站提交这些内容」说得更明确；**用前确认域名是唯一有效的防线**。

### R2【中 · 设计使然】简历全文发给 AI 服务商

`popup.js:928` 把简历原文前 12000 字整段拼进 prompt 做结构化抽取；生成面试方案 / 自我介绍 / 简历优化时（popup.js:2935 / 2943 / 2945）还会再发一次。简历里若写了身份证、住址、手机，就一并交给了所配置的服务商。

**关键规避手段**：`popup.js:918` 的 `useAi = Boolean(settings.apiUrl && settings.apiKey && RESUME_EXTRACT_PROMPT)`。**不配置接口地址和 Key，简历解析会走纯本地规则（`extractProfileLocally`），简历原文完全不出设备。** AI 失败时也会回退本地（标记为 `local-fallback`）。

**建议**：保持这条本地分支永远可用，任何改动都不能让它退化成「必须联网」。

### R3【中 · v1.6.0 新引入】字段表明文持久落盘

`profile.js:1945` 把整个字段表写进 `chrome.storage.local`。用户在字段表里填过的一切（含敏感字段值，若用户填了）都会明文躺在浏览器 profile 目录，同机其他程序 / 用户可读。

这是 MV3 标准做法，无加密方案可在不引入后台密钥管理的前提下根除。

**建议**：知情即可；界面上已有「清空字段表」入口（popup.js:1028 附近，清空后简历原文仍保留）。**在不属于自己管理的电脑上使用后，应清空字段表**。

### R5【中 · 旧报告遗留】权限面

`host_permissions` 与 `content_scripts.matches` 均为 `http://*/*` + `https://*/*`。`http://` 是为本地 `127.0.0.1:8080` 的 llama.cpp 服务保留的，不能直接删。副作用是所有明文 http 页面（可被中间人篡改）也会被注入内容脚本。

`all_frames: true` 仅用于复制保护脚本（copy-guard 系列），会注入广告 / 第三方 iframe。

**建议**：若复制保护只需作用于顶层文档，去掉该处的 `all_frames`；`http://` 保留但可考虑收窄到本地网段。

### R6【低-中 · 旧报告遗留】第三方库无版本

`lib/pdf.min.js`（642KB 级）、`lib/mammoth.min.js`、`lib/pdf.worker.min.js` 为手工放入，无 `package.json` / lockfile，无法确认是否含已知 CVE。pdf.js 历史上多次出现「解析恶意 PDF 触发漏洞」的案例，而用户上传的 PDF 正是在本地被它解析 —— 这是**唯一一处「处理不可信输入」**的组件。

**建议**：升级到当前稳定版（`pdf.min.js` 与 `pdf.worker.min.js` 必须同版本），并在 `lib/` 下加一个 README 标注版本与来源。

### R7【中 · 旧报告遗留】MAIN 世界的原型覆盖

`copy-guard-main.js:23` 覆盖 `Event.prototype.preventDefault`，`:31` 覆盖 `returnValue`。副作用：当复制保护开启时，目标页自身在这些事件类型上的合法拦截（拖拽锁定、自定义选区）会被一并绕过，可能破坏页面功能。

功能理解：`enabled === true` 时放行 `copy/cut/selectstart/contextmenu/dragstart` 与 `Ctrl+A/C/X`，即**帮用户解除站点的复制限制**。页面虽能自行派发 `__interviewCopyGuard` 事件改变此状态，但结果只是放行复制，对用户无害，**不构成安全问题**。

**建议**：保留 ISOLATED 世界的 `stopImmediatePropagation` 方案（已在捕获阶段拦截，多数场景够用），去掉 MAIN 世界的原型覆盖；或收窄为仅拦截同名事件、不碰原型链。

### R8【低】API Key 明文落盘

MV3 标准行为。输入框已用 `type=password`。

### R9【低】导出文件

`popup.js:2685` 用 Blob 生成本地 HTML 并触发下载，内容是基于简历生成的面试方案 / 自我介绍。**需要用户主动点击才会产生**，不由扩展自动外发。

---

## 六、已经做对的地方（改动时请勿破坏）

这些是本项目的隐私资产，逐条都已核对代码：

**6.1 敏感字段默认不填**
`PROFILE_SENSITIVE_KEYS` 由 `sensitive: true` 标记动态收集（profile.js:445），共 **4 项**：

| 字段 | key | 位置 |
|---|---|---|
| 证件号码（身份证） | `basic.idCard` | profile.js:25 |
| 银行卡号 | `basic.bankAccount` | profile.js:41 |
| 家庭住址 | `basic.homeAddress` | profile.js:42 |
| 紧急联系人 | `basic.emergencyContact` | profile.js:43 |

开关 `#autofill-sensitive` 在 popup.html:263 **无 `checked` 属性 = 默认关闭**。`#autofill-overwrite`（覆盖页面已有内容）同样默认关闭。

**6.2 最小外发**
- AI 字段映射只发页面控件结构，不含用户数据（popup.js:1429）
- `form:fill` 只发已匹配的字段值 `{ref, value}`，**不发整个字段表**（popup.js:1810）
- 预填链路的两个文件零 `fetch`

**6.3 页面侧不留数据痕迹**
`markField`（form-fill-page.js:693）只在控件上加一个 CSS class，**不往 DOM 属性写任何用户数据** —— 页面脚本读不到「这个扩展填了什么」之外的额外信息。

**6.4 简历原文不持久化**
简历原文走 `storage.session`（popup.js:195），关浏览器即清空；只有结构化后的字段表才落 `storage.local`。

**6.5 旧报告 P0 已修**

| 旧编号 | 问题 | 修复 |
|---|---|---|
| S2 | fetch 跟随重定向 → API Key 可能 302 泄露到其他主机 | `redirect: 'error'`（popup.js:2771、options.js:94） |
| S3 | Referer 把当前招聘页 URL 泄露给 AI 服务商 | `referrer: 'no-referrer'`（同上两处） |
| S1 | API 地址只校验格式，不限协议 | `validateApiUrl`（options.js:4-13）：强制 https，http 仅限 `localhost` / `127.0.0.1` / `::1` / `192.168.*` / `10.*` / `*.local` |

---

## 七、明确不是问题的点（避免误报）

- **XSS（AI 输出经 innerHTML）**：`formatBody` 先 escapeHtml 再注入 `<strong>/<em>`，已确认安全。**但顺序脆弱** —— 未来若有人调换顺序会瞬间变成 XSS，建议在函数顶部加注释固定该顺序。
- **eval / new Function / 内联 on\* 事件**：代码中无使用，manifest 默认 CSP 足够。
- **CSRF / 权限提升**：本次修复后，4 处消息入口均只接受本扩展消息。
- **数据外传到未经配置的第三方**：全项目仅 2 处网络请求，目标均为用户自己配置的接口地址。
- **同步 XHR（prompts.js）**：使用 `chrome.runtime.getURL` 读扩展内部文件，同源，非网络请求。
- **MAIN 世界事件伪造**：页面可自行派发 `__interviewCopyGuard` 改变复制保护状态，但结果只是放行复制，对用户无害。
- **`chrome.runtime.onConnect`（background.js:80）**：port 名 `sidePanel`，仅本扩展侧边栏能建立。

---

## 八、修复优先级

| 优先级 | 项 | 改动量 | 状态 |
|---|---|---|---|
| **已完成** | R4 消息入口 sender 校验（4 处）+ 12 条防回退断言 | 4 行 ×4 + 测试 | ✅ v1.6.1 |
| P1 | R6 升级 pdf.js / mammoth 并标注版本 | 替换文件 | 待办（需回归 PDF / DOCX 解析） |
| P1 | R1 界面明确「预填会向当前网站提交这些内容」 | 文案 | 待办 |
| P2 | R7 去掉 MAIN 世界原型覆盖 | 重写脚本 | 待办（需验证复制保护仍生效） |
| P2 | R5 视情况去掉复制保护的 `all_frames` | manifest | 待办 |
| — | R2 / R3 / R8 / R9 | — | 知悉即可 |

---

## 九、给使用者的三条实操建议

1. **只在确认过域名的正规网申页点预填**。预填的动作本质就是把这些内容交给当前页面，这是 R1，无法用代码消除。
2. **想彻底不联网：清空设置页的接口地址和 Key**。简历解析会退回纯本地规则，照样能用 —— 这是本项目最强的隐私开关（popup.js:918）。
3. **在公共 / 他人电脑上用过之后，清空字段表**（简历原文会保留）。字段表现在是明文持久化的（R3）。

---

## 十、一句话总结

**代码没有可被远程利用的漏洞**；风险集中在「你把数据交给了谁」—— 简历全文交给 AI 服务商（可关闭）、字段表明文落盘（v1.6.0 新引入）、预填即向目标网站交出数据（功能本质）。加固项里最该做的 sender 校验已于 v1.6.1 修复，剩下的是供应链（pdf.js 版本）与两条历史遗留的权限 / 副作用问题。
