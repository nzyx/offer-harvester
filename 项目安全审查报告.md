# 面邀收割机 · 项目安全审查报告

> 审查对象：Manifest V3 侧边栏扩展（v1.0.6）
> 审查范围：全部源码（popup.js / options.js / background.js / copy-guard*.js / prompts.js / manifest.json / 两个 html / lib 第三方库）
> 审查日期：2026-08-26
> 结论：**无高危可利用漏洞（XSS / 注入 / RCE 均不在本地代码中触发）；存在 4 项中风险（密钥传输面、原型覆盖副作用、第三方库版本不明、重定向泄露）和若干低风险项，均为"加固"级别，不影响当前能跑。**

---

## 一、风险总览

| # | 问题 | 位置 | 严重度 | 是否需要改 |
|---|---|---|---|---|
| S1 | API Key 明文存本地 + 仅校验 URL 格式，无协议/域名约束 | options.js:5-9, 26-38 | 中 | 建议改 |
| S2 | fetch 未禁止重定向跟随 → Key 可经 302 泄露到意料之外的主机 | popup.js:517-521 / options.js:68-72 | **中** | **建议改** |
| S3 | fetch 未设 Referrer-Policy → 当前页 URL（招聘页）可能泄露给 AI 服务商 | popup.js:517-521 / options.js:68-72 | 低-中 | 建议改 |
| S4 | MAIN 世界全局重写 `Event.prototype.preventDefault` / `returnValue` | copy-guard-main.js:23,31 | 中 | 看取舍 |
| S5 | host_permissions / content_scripts 用 `<all_urls>` + `all_frames:true` | manifest.json:22-49 | 中 | 看取舍 |
| S6 | 第三方库（pdf.js / mammoth）无版本声明、无 lockfile | lib/ | 低-中 | 建议改 |
| S7 | chrome.storage.local 存 API Key 不加密 | 全局 | 低（标准做法） | 知悉即可 |
| S8 | formatBody 渲染先转义后注入标签——当前安全，但顺序一反即 XSS | popup.js:315-328 | 低（维护脆弱） | 知悉即可 |

---

## 二、逐项详情与修复建议

### S2【中】fetch 重定向会导致 API Key 泄露 ★ 最该修
**现象**：`callApi` 与测试连接都用 `fetch(url, {headers:{Authorization: Bearer <key>}})`，**默认跟随 3xx 重定向**。若用户把接口地址填成任意一个会 302 跳转的 URL（无论有意还是被钓鱼），Key 会被原样带去跳转后的新主机。

**修复**（两处都要加）：
```js
const response = await fetch(completionUrl, {
  method: 'POST', signal: controller.signal,
  redirect: 'error',                 // 禁止跟随重定向
  referrer: 'no-referrer',           // 见 S3
  headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${settings.apiKey}` },
  body: JSON.stringify(body)
});
```
`redirect: 'error'` 时遇到 3xx 会直接抛异常，Key 不出本域。`options.js` 的测试连接同样处理。

### S3【低-中】Referrer 泄露当前页面
**现象**：fetch 默认带 `Referer/Origin`。用户在招聘页打开侧边栏时，请求会带上该招聘页 URL，泄露给 AI 服务商。
**修复**：如上加 `referrer: 'no-referrer'`。

### S1【中】API Key 存储与 URL 校验过宽
**现象**：
1. Key 存 `chrome.storage.local`（明文，落盘于用户 profile 目录）。同机其他用户/恶意软件可读——这是 MV3 标准做法，难以根除，知悉即可。
2. 保存/测试时只 `new URL()` 校验格式，**不限制协议、不提示域名归属**。用户可能被诱导指向恶意域名，Key 明文发过去。

**修复**（保留 http 以支持你的本地 llama.cpp `127.0.0.1:8080`，但收窄到安全主机）：
```js
function isSafeUrl(u) {
  const url = new URL(u);
  if (url.protocol !== 'https:' && url.protocol !== 'http:') return false;   // 禁止 file:/javascript:/data:
  // 允许本地/私有地址（你自己的 LLM），公网 http 给出警告但不阻断
  const host = url.hostname;
  const isLocal = host === 'localhost' || host === '127.0.0.1' || host === '[::1]' ||
                  /^192\.168\./.test(host) || /^10\./.test(host) || /^172\.(1[6-9]|2\d|3[01])\./.test(host);
  if (url.protocol === 'http:' && !isLocal) {
    return 'warn';   // 提示"明文 http 公网传输，Key 可能被窃听"
  }
  return true;
}
```
并在 `save-settings` 时若返回 `'warn'`，状态栏提示一次即可，仍允许保存（兼顾本地推理）。

### S4【中】MAIN 世界原型覆盖的副作用
**现象**：`copy-guard-main.js` 在页面主世界覆盖 `Event.prototype.preventDefault`，使所有 `copy/cut/selectstart/contextmenu` 及 `Ctrl+C/X/A` 的 `preventDefault` 失效。**副作用**：当复制保护开启时，若目标页本身在以上事件类型上做合法拦截（如拖拽锁定、自定义选区逻辑），会被一并绕过，可能破坏页面功能。此外这类"绕过站点技术措施"的扩展在 Chrome Web Store 上架会被拒（自用/本地加载无妨）。

**建议**：保留现有 ISOLATED 世界的 `stopImmediatePropagation` 方案（已在捕获阶段拦截，足够解除多数复制限制），**去掉 MAIN 世界的原型覆盖**，或把原型覆盖收窄为仅 `addEventListener` 捕获阶段拦截同名事件，不碰原型链。功能上几乎无损，兼容性更好。

### S5【中】权限面过宽
**现象**：`host_permissions` 与 `content_scripts.matches` 都为 `http://*/*` + `https://*/*`，且 `all_frames: true` 注入到每个 iframe。`http://` 是给你的本地 LLM 留的，不能删；但 `all_frames: true` 会把脚本灌进广告/第三方子框架，行为面过大。

**建议**：若复制保护只需作用于顶层文档，去掉 `all_frames`；保留 `http://*/*`+`https://*/*`（你的本地推理依赖 http）。

### S6【低-中】第三方库无版本 & 供应链
**现象**：`lib/pdf.min.js`、`lib/mammoth.min.js` 手工放入，无 `package.json`/lockfile，无法确认是否含已知 CVE。pdf.js 历史上多次有解析恶意 PDF 的漏洞。用户上传的 PDF 在本地用 pdf.js 解析——旧版可能被触发。

**建议**：升级到最新稳定版 pdf.js（同版本 pdf.worker.min.js 配套）与 mammoth；在 `lib/` 加 `README` 或 `package.json` 标注版本号，便于复现与审计。

### S7【低】Key 落盘明文
标准 MV3 行为，无法在不引入后台加密的情况下根除。可接受的折中：在 `options.html` 的 API Key 输入框加 `autocomplete="off"` 并避免把明文回填到可见字段（当前已用 `type=password`，OK）。

### S8【低】渲染顺序脆弱但当前安全
`formatBody` 当前是 **先 escapeHtml 再注入 `<strong>/<em>`**，AI 文本里的标签不会执行，已确认 XSS-safe。唯一风险是未来有人把顺序调反会瞬间变 XSS。建议在函数顶部加注释固定顺序；若要彻底免疫，可改用 `document.createElement` + `textContent` 构建 DOM 而非 `innerHTML` 拼接。

---

## 三、明确"不是问题"的点（避免误报）

- ❌ **XSS（AI 输出 innerHTML）**：已转义，安全。
- ❌ **eval / new Function / 内联 on* 事件**：代码无使用，manifest 默认 CSP 足够。
- ❌ **CSRF / 权限提升**：`chrome.runtime.onMessage` 仅响应自己 content script 的消息，外部页无法伪造。
- ❌ **数据外传**：JD/简历/结果存 `storage.session`（关浏览器清空），不上传；仅 API Key + 你主动提交给配置的 AI 接口。
- ❌ **同步 XHR（prompts.js）**：用 `chrome.runtime.getURL` 取扩展内部文件，同源，非网络请求，安全（仅 `file://` 直接打开页面时会被浏览器拦截，属预期）。
- ❌ **loading 模板注入**：`loading-template` 为静态内容，安全。

---

## 四、推荐修复优先级

| 优先级 | 项 | 改动量 | 风险 |
|---|---|---|---|
| P0 | S2 重定向 `redirect:'error'` | 2 行 ×2 处 | 零（纯加固） |
| P0 | S3 `referrer:'no-referrer'` | 1 行 ×2 处 | 零 |
| P1 | S1 URL 协议/本地校验 + 公网 http 警告 | ~15 行 | 低 |
| P1 | S6 升级并标注库版本 | 替换文件 | 低（需回归测试 PDF/DOCX 解析） |
| P2 | S4 去掉 MAIN 世界原型覆盖 | 重写脚本 | 中（需验证复制保护仍生效） |
| P2 | S5 视情况去 `all_frames` | manifest | 低 |

> 注：P0 两项我可以直接帮你改，零功能回归风险。S4/S5 涉及复制保护核心行为，建议先在你的常用招聘站验证后再合入。

---

## 五、一句话总结

**代码本身没有可被远程利用的安全漏洞**；主要风险集中在"API Key 的传输与存储面"和"复制保护脚本的权限/副作用面"，都是加固项，按 P0→P2 顺序修即可。你本地的 `127.0.0.1:8080` llama.cpp 场景已被纳入方案（保留 http，仅对公网明文 http 给警告）。
